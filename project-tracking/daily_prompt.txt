Read project-tracking/cert_kb_queue.md. Find the FIRST unchecked [ ] item in the Phase A
section. For that ONE item only, do the following, in order, and then stop:

1. Audit current KB content for that product section — what already exists today.
2. Precheck: research via WebSearch whether a real, named, published certification exam
   exists for this product. If yes, find and verify its official objectives/blueprint from
   a primary source (the vendor's own site, preferred over third-party summaries). If no
   formal certification exists for this product, define a reasonable fallback "expert
   level" bar instead (the vendor's own official training curriculum, or a well-regarded
   community standard) and say so explicitly.
3. Write the findings as a note directly under that item in
   project-tracking/cert_kb_queue.md (certification name + blueprint source URL, or
   "no formal cert — fallback: X").
4. Test/verify: re-read what you wrote. Confirm the facts and any URLs are accurate before
   moving on.
5. Run python3 scripts/site_audit.py in full.
6. If it is not 100% clean (excluding the two known standing exceptions — Check 28 admin
   staleness and Check 59's documented soft heuristic) AND the new issue is related to
   anything you touched: add a permanent check for it to scripts/site_audit.py if it's a
   new class of bug (per the standing bug-to-audit rule), fix the underlying issue, and
   re-run the audit to confirm clean before proceeding.
7. Mark the queue item as [x] done in project-tracking/cert_kb_queue.md.
8. Commit everything from this run with a clear message, and push.
9. STOP. Do not start a second Phase A item in this run, even if there is time or token
   budget left — one item per day, by design, to keep this sustainable over months.

If anything is ambiguous or you get stuck (can't find reliable info on a cert, unsure how
to classify a product, etc.), do not guess destructively. Leave a clear, honest note in
the queue file about what's blocking it, commit that note, and stop — better to skip a day
cleanly than push something wrong.
