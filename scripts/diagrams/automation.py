"""Automation section diagrams — root, Ansible, GitHub Actions, PowerShell, Python, Terraform."""
from ._core import kb_diagram, make_helpers, bTop, bMid, bBot, sections, merge, arrow, connector, title_border

W2 = 103


@kb_diagram('automation/index', 'docs/automation/index.md', 'Automation overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Automation — Infrastructure Automation Tools'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Automation: codifying repeatable infrastructure operations into executable, version-controlled logic')))
    lines.append(R(bMid(3, 99, 'Goals: eliminate manual error, enforce consistency, accelerate change delivery, enable self-service')))
    lines.append(R(bMid(3, 99, 'Tools covered: Ansible (config mgmt), Terraform (IaC), GitHub Actions (CI/CD), PowerShell, Python')))
    lines.append(R(bMid(3, 99, 'Maturity path: manual → scripted → idempotent config mgmt → full IaC with pipeline gates')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Configuration Management'), bMid(53, 99, 'Infrastructure as Code'))))
    lines.append(R(merge(bMid(3, 50, 'Ansible: agentless SSH/WinRM'), bMid(53, 99, 'Terraform: declarative HCL'))))
    lines.append(R(merge(bMid(3, 50, 'Playbooks → roles → collections'), bMid(53, 99, 'Plan → apply → state tracking'))))
    lines.append(R(merge(bMid(3, 50, 'AWX/AAP: enterprise control plane'), bMid(53, 99, 'Multi-cloud and on-prem providers'))))
    lines.append(R(merge(bMid(3, 50, 'Idempotent: safe to re-run'), bMid(53, 99, 'Remote state: S3, Terraform Cloud'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'CI/CD Pipelines'), bMid(53, 99, 'Scripting Languages'))))
    lines.append(R(merge(bMid(3, 50, 'GitHub Actions: event-driven jobs'), bMid(53, 99, 'PowerShell: Windows + cross-platform'))))
    lines.append(R(merge(bMid(3, 50, 'Runners: GitHub-hosted or self-hosted'), bMid(53, 99, 'Python: scripting, APIs, SDKs'))))
    lines.append(R(merge(bMid(3, 50, 'Secrets, environments, approvals'), bMid(53, 99, 'Virtual envs, pip, Poetry'))))
    lines.append(R(merge(bMid(3, 50, 'Marketplace actions + custom steps'), bMid(53, 99, 'boto3, paramiko, ansible, requests'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical Infrastructure (what automation manages):')))
    lines.append(R(bMid(3, 99, 'Bare-metal servers · VMs · containers · cloud VMs · network devices · storage arrays')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible         = agentless configuration management; SSH/WinRM transport; YAML playbooks')))
    lines.append(R(bMid(3, 99, 'Terraform       = HashiCorp IaC tool; provider plugins; declarative resource graph')))
    lines.append(R(bMid(3, 99, 'GitHub Actions  = native GitHub CI/CD; workflow YAML in .github/workflows/')))
    lines.append(R(bMid(3, 99, 'PowerShell      = Microsoft shell and scripting language; .ps1 files; PS7 cross-platform')))
    lines.append(R(bMid(3, 99, 'Python          = general-purpose language; dominant for infra scripting and API automation')))
    lines.append(R(bMid(3, 99, 'Idempotent      = running the same automation multiple times produces the same result')))
    lines.append(R(bMid(3, 99, 'AWX / AAP       = Ansible Automation Platform; web UI + API + RBAC over Ansible Engine')))
    lines.append(R(bMid(3, 99, 'IaC             = Infrastructure as Code; resources declared in files, tracked in git')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/index', 'docs/automation/ansible/index.md', 'Ansible overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Agentless Configuration Management'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible: open-source agentless automation using SSH (Linux) or WinRM (Windows) transport')))
    lines.append(R(bMid(3, 99, 'Control node pushes tasks to managed nodes; no agent installation required on targets')))
    lines.append(R(bMid(3, 99, 'YAML playbooks describe desired state; modules execute tasks; idempotent by design')))
    lines.append(R(bMid(3, 99, 'AWX / AAP adds RBAC, scheduling, credential vault, and REST API over the Ansible engine')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Architecture'), bMid(36, 66, 'Operations'), bMid(69, 99, 'Security'))))
    lines.append(R(merge(bMid(3, 33, 'Control node + SSH'), bMid(36, 66, 'Inventory management'), bMid(69, 99, 'Vault secrets'))))
    lines.append(R(merge(bMid(3, 33, 'Inventory static/dynamic'), bMid(36, 66, 'Playbook execution'), bMid(69, 99, 'SSH key auth'))))
    lines.append(R(merge(bMid(3, 33, 'Roles and collections'), bMid(36, 66, 'AWX job templates'), bMid(69, 99, 'RBAC via AWX'))))
    lines.append(R(merge(bMid(3, 33, 'AWX/AAP control plane'), bMid(36, 66, 'Health and upgrade'), bMid(69, 99, 'Credential store'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Playbook      = ordered list of plays; each play maps a set of hosts to a set of tasks')))
    lines.append(R(bMid(3, 99, 'Role          = reusable unit: tasks, handlers, vars, files, templates, defaults, meta')))
    lines.append(R(bMid(3, 99, 'Collection    = distribution format for roles + modules + plugins; installed via ansible-galaxy')))
    lines.append(R(bMid(3, 99, 'Inventory     = list of managed nodes; static (INI/YAML) or dynamic (scripts, plugins)')))
    lines.append(R(bMid(3, 99, 'Module        = unit of work called by a task; e.g. apt, yum, copy, template, service')))
    lines.append(R(bMid(3, 99, 'Handler       = task triggered only when notified by another task; used for service restarts')))
    lines.append(R(bMid(3, 99, 'Vault         = Ansible encryption for secrets; ansible-vault encrypt/decrypt')))
    lines.append(R(bMid(3, 99, 'AWX           = Ansible Tower upstream; web UI + API + RBAC + scheduling')))
    lines.append(R(bMid(3, 99, 'AAP           = Ansible Automation Platform; Red Hat enterprise product; includes AWX')))
    lines.append(R(bMid(3, 99, 'Galaxy        = community hub for roles and collections; galaxy.ansible.com')))
    lines.append(R(bMid(3, 99, 'host_vars     = variables scoped to a single host; stored in host_vars/<hostname>/')))
    lines.append(R(bMid(3, 99, 'group_vars    = variables scoped to a group; stored in group_vars/<groupname>/')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/architecture/index', 'docs/automation/ansible/architecture/index.md', 'Ansible architecture overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Architecture'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible architecture: control node runs ansible-playbook; connects outbound via SSH/WinRM')))
    lines.append(R(bMid(3, 99, 'No persistent agent on managed nodes; Python must be present on Linux targets (>=2.7/3.5)')))
    lines.append(R(bMid(3, 99, 'AWX provides a stateful control plane: job queue, credential store, event logging')))
    lines.append(R(bMid(3, 99, 'Execution environments (EE): container images bundling Ansible + dependencies')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'How It Works'), bMid(53, 99, 'Design Standards'))))
    lines.append(R(merge(bMid(3, 50, 'Inventory → play → task → module'), bMid(53, 99, 'One role per concern'))))
    lines.append(R(merge(bMid(3, 50, 'SSH multiplexing for speed'), bMid(53, 99, 'Tag every task for selective runs'))))
    lines.append(R(merge(bMid(3, 50, 'Facts gathered per host'), bMid(53, 99, 'Roles in collections, not loose'))))
    lines.append(R(merge(bMid(3, 50, 'Forks: parallel host execution'), bMid(53, 99, 'No hardcoded credentials'))))
    lines.append(R(merge(bMid(3, 50, 'Handlers deferred to play end'), bMid(53, 99, 'Use check mode in pipelines'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Integrations'), bMid(53, 99, 'Execution Model'))))
    lines.append(R(merge(bMid(3, 50, 'AWX REST API → CI/CD triggers'), bMid(53, 99, 'Control node → SSH → managed'))))
    lines.append(R(merge(bMid(3, 50, 'GitHub Actions calls AWX API'), bMid(53, 99, 'Execution environment (EE)'))))
    lines.append(R(merge(bMid(3, 50, 'HashiCorp Vault for secrets'), bMid(53, 99, 'Forks = parallel host limit'))))
    lines.append(R(merge(bMid(3, 50, 'LDAP/AD → AWX RBAC groups'), bMid(53, 99, 'Callback plugins: log results'))))
    lines.append(R(merge(bMid(3, 50, 'Dynamic inventory: AWS, vSphere'), bMid(53, 99, 'Fact cache: Redis or file'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical: control node host (VM or container) + network access to managed nodes on port 22')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/architecture/how-it-works', 'docs/automation/ansible/architecture/how-it-works/index.md', 'Ansible how it works')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — How It Works'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Execution flow: ansible-playbook reads inventory + playbook → connects to hosts → runs tasks')))
    lines.append(R(bMid(3, 99, 'Each task calls a module; module code is transferred to the remote host and executed')))
    lines.append(R(bMid(3, 99, 'Results returned as JSON; Ansible evaluates changed/ok/failed; handlers notified on changed')))
    lines.append(R(bMid(3, 99, 'AWX wraps this: stores credentials, queues jobs, streams events to UI in real time')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Step 1: Parse'), bMid(36, 66, 'Step 2: Connect'), bMid(69, 99, 'Step 3: Execute'))))
    lines.append(R(merge(bMid(3, 33, 'Load inventory'), bMid(36, 66, 'SSH/WinRM open'), bMid(69, 99, 'Module runs on host'))))
    lines.append(R(merge(bMid(3, 33, 'Parse playbook YAML'), bMid(36, 66, 'Gather facts (setup)'), bMid(69, 99, 'JSON result returned'))))
    lines.append(R(merge(bMid(3, 33, 'Resolve variables'), bMid(36, 66, 'Multiplexed SSH conn'), bMid(69, 99, 'changed / ok / failed'))))
    lines.append(R(merge(bMid(3, 33, 'Apply filters/limits'), bMid(36, 66, 'Fork N hosts parallel'), bMid(69, 99, 'Handlers triggered'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Gather facts     = module: setup; collects OS, IP, memory, disk, fqdn for use in templates')))
    lines.append(R(bMid(3, 99, 'Check mode       = --check flag; simulates changes without applying; modules report what would change')))
    lines.append(R(bMid(3, 99, 'Diff mode        = --diff flag; shows before/after for file edits; combine with --check')))
    lines.append(R(bMid(3, 99, 'Fork             = max parallel host connections; default 5; increase for large inventories')))
    lines.append(R(bMid(3, 99, 'Serial           = limit hosts per batch in rolling update; e.g. serial: 1 for canary deploys')))
    lines.append(R(bMid(3, 99, 'delegate_to      = run task on a different host (e.g. register DNS on a jump host)')))
    lines.append(R(bMid(3, 99, 'when             = conditional expression; task skipped when condition is False')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/architecture/design-standards', 'docs/automation/ansible/architecture/design-standards/index.md', 'Ansible design standards')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Design Standards'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible design standards enforce consistency, security, and maintainability across playbooks')))
    lines.append(R(bMid(3, 99, 'Rules: no bare variables, no hardcoded credentials, roles over monolithic playbooks')))
    lines.append(R(bMid(3, 99, 'Lint with ansible-lint; molecule for role testing; yamllint for formatting')))
    lines.append(R(bMid(3, 99, 'All secrets in Vault or AWX credential store — never in plain text in repo')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Repo and File Structure'), bMid(53, 99, 'Coding Standards'))))
    lines.append(R(merge(bMid(3, 50, 'inventories/prod/, staging/'), bMid(53, 99, 'Name all tasks descriptively'))))
    lines.append(R(merge(bMid(3, 50, 'roles/ at repo root or collection'), bMid(53, 99, 'Use FQCN for all modules'))))
    lines.append(R(merge(bMid(3, 50, 'group_vars/ and host_vars/'), bMid(53, 99, 'Tag tasks: --tags deploy,config'))))
    lines.append(R(merge(bMid(3, 50, 'playbooks/ top-level entry points'), bMid(53, 99, 'Use become only when required'))))
    lines.append(R(merge(bMid(3, 50, '.ansible-lint, .yamllint configs'), bMid(53, 99, 'Validate with check mode first'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'FQCN          = Fully Qualified Collection Name; e.g. ansible.builtin.copy not just copy')))
    lines.append(R(bMid(3, 99, 'ansible-lint  = static analysis for playbooks and roles; enforces best-practice rules')))
    lines.append(R(bMid(3, 99, 'molecule      = role testing framework; creates instances, converges, verifies, destroys')))
    lines.append(R(bMid(3, 99, 'yamllint      = YAML syntax and style linter; catches indentation and quoting errors')))
    lines.append(R(bMid(3, 99, 'become        = privilege escalation (sudo); apply at task level, not play level where possible')))
    lines.append(R(bMid(3, 99, 'no_log: true  = suppress task output for tasks handling passwords or secrets')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/architecture/integrations', 'docs/automation/ansible/architecture/integrations/index.md', 'Ansible integrations')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Integrations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible integrates with CI/CD pipelines, secrets managers, ITSM tools, and cloud platforms')))
    lines.append(R(bMid(3, 99, 'AWX REST API is the primary integration point for triggering playbooks from external systems')))
    lines.append(R(bMid(3, 99, 'Dynamic inventory plugins connect to AWS, vSphere, Netbox, GCP, Azure for live host lists')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'CI/CD'), bMid(36, 66, 'Secrets'), bMid(69, 99, 'Inventory Sources'))))
    lines.append(R(merge(bMid(3, 33, 'GitHub Actions → AWX'), bMid(36, 66, 'HashiCorp Vault'), bMid(69, 99, 'AWS EC2 plugin'))))
    lines.append(R(merge(bMid(3, 33, 'GitLab CI → AWX API'), bMid(36, 66, 'CyberArk PAM'), bMid(69, 99, 'VMware vSphere'))))
    lines.append(R(merge(bMid(3, 33, 'Jenkins → AWX webhook'), bMid(36, 66, 'AWX credential types'), bMid(69, 99, 'Netbox plugin'))))
    lines.append(R(merge(bMid(3, 33, 'Webhook triggers in AWX'), bMid(36, 66, 'Ansible Vault (static)', ), bMid(69, 99, 'OpenStack plugin'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Dynamic inventory = script or plugin that queries an external source for host lists at runtime')))
    lines.append(R(bMid(3, 99, 'Webhook trigger   = AWX can receive HTTP webhooks from GitHub/GitLab to auto-run job templates')))
    lines.append(R(bMid(3, 99, 'Credential type   = AWX pluggable credential; stores SSH keys, API tokens, Vault tokens securely')))
    lines.append(R(bMid(3, 99, 'Netbox            = open-source DCIM/IPAM; Ansible inventory plugin pulls live device list')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/index', 'docs/automation/ansible/operations/index.md', 'Ansible operations overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Operations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible operations: running playbooks, managing inventory, AWX job templates, upgrades')))
    lines.append(R(bMid(3, 99, 'Day-to-day: add/remove hosts from inventory, update group_vars, trigger jobs via AWX UI or API')))
    lines.append(R(bMid(3, 99, 'Health checks: AWX service status, job success rate, credential expiry, EE image currency')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'CLI Operations'), bMid(36, 66, 'AWX Operations'), bMid(69, 99, 'Maintenance'))))
    lines.append(R(merge(bMid(3, 33, 'ansible-playbook run'), bMid(36, 66, 'Job template launch'), bMid(69, 99, 'Upgrade AWX'))))
    lines.append(R(merge(bMid(3, 33, 'ansible-inventory --list'), bMid(36, 66, 'Inventory sync'), bMid(69, 99, 'Update EE images'))))
    lines.append(R(merge(bMid(3, 33, 'ansible -m ping all'), bMid(36, 66, 'Credential rotation'), bMid(69, 99, 'Rotate Vault keys'))))
    lines.append(R(merge(bMid(3, 33, 'ansible-vault rekey'), bMid(36, 66, 'RBAC team assignment'), bMid(69, 99, 'Purge old job data'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Job template    = saved configuration: playbook + inventory + credentials + extra vars in AWX')))
    lines.append(R(bMid(3, 99, 'Inventory sync  = pull current host list from dynamic source (cloud, Netbox) into AWX')))
    lines.append(R(bMid(3, 99, 'EE              = Execution Environment; OCI container with Ansible + collections + Python deps')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/backup-restore', 'docs/automation/ansible/operations/backup-restore/index.md', 'Ansible backup and restore')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Backup & Restore'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible backup: the source of truth is the git repository — playbooks, roles, inventory, vars')))
    lines.append(R(bMid(3, 99, 'AWX state: export via awx CLI or AWX API; includes job templates, credentials, inventories')))
    lines.append(R(bMid(3, 99, 'Restore: re-deploy AWX (Kubernetes operator or RPM), re-import exported objects, reconfigure')))
    lines.append(R(bMid(3, 99, 'Critical: Vault password must be stored out-of-band (password manager, HSM) — not in git')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'What to Back Up'), bMid(53, 99, 'Restore Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Git repo (all automation code)'), bMid(53, 99, '1. Redeploy AWX instance'))))
    lines.append(R(merge(bMid(3, 50, 'AWX export: awx export --all'), bMid(53, 99, '2. Import AWX objects from export'))))
    lines.append(R(merge(bMid(3, 50, 'Vault password (offline safe)'), bMid(53, 99, '3. Restore Vault password'))))
    lines.append(R(merge(bMid(3, 50, 'AWX PostgreSQL DB snapshot'), bMid(53, 99, '4. Verify job template runs OK'))))
    lines.append(R(merge(bMid(3, 50, 'Execution environment OCI images'), bMid(53, 99, '5. Test connectivity to targets'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'awx CLI     = command-line client for AWX API; awx export --all dumps all objects to JSON')))
    lines.append(R(bMid(3, 99, 'AWX DB      = PostgreSQL database; stores job history, credentials (encrypted), config')))
    lines.append(R(bMid(3, 99, 'Vault PW    = Ansible Vault encryption password; if lost, all vault-encrypted vars are unreadable')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/cli-reference', 'docs/automation/ansible/operations/cli-reference/index.md', 'Ansible CLI reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — CLI Reference'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible CLI tools: ansible, ansible-playbook, ansible-inventory, ansible-vault, ansible-galaxy')))
    lines.append(R(bMid(3, 99, 'ansible-playbook: primary command; runs a playbook against an inventory')))
    lines.append(R(bMid(3, 99, 'Common flags: -i (inventory), -l (limit), --tags, --check, --diff, -v/-vvv (verbosity)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'ansible-playbook'), bMid(53, 99, 'ansible (ad-hoc)'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-playbook site.yml'), bMid(53, 99, 'ansible all -m ping'))))
    lines.append(R(merge(bMid(3, 50, '  -i inventories/prod/'), bMid(53, 99, 'ansible web -m shell -a uptime'))))
    lines.append(R(merge(bMid(3, 50, '  -l webservers --tags deploy'), bMid(53, 99, 'ansible db -m setup'))))
    lines.append(R(merge(bMid(3, 50, '  --check --diff'), bMid(53, 99, 'ansible all -m reboot -b'))))
    lines.append(R(merge(bMid(3, 50, '  -e "version=1.2.3"'), bMid(53, 99, '  -b = become (sudo)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault'), bMid(53, 99, 'ansible-galaxy'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault encrypt vars.yml'), bMid(53, 99, 'ansible-galaxy install <role>'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault decrypt vars.yml'), bMid(53, 99, 'ansible-galaxy collection install'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault rekey vars.yml'), bMid(53, 99, 'ansible-galaxy list'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault view vars.yml'), bMid(53, 99, 'requirements.yml for pinned deps'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, '-v / -vvv   = verbosity levels; -vvv shows module args and return values for debugging')))
    lines.append(R(bMid(3, 99, '--limit / -l= restrict to matching hosts or groups; supports patterns: web[1:3], !db')))
    lines.append(R(bMid(3, 99, '--extra-vars = -e "key=value"; highest precedence; override any inventory/role variable')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/health-checks', 'docs/automation/ansible/operations/health-checks/index.md', 'Ansible health checks')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Health Checks'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible health checks: verify control node, AWX services, connectivity, and job success rates')))
    lines.append(R(bMid(3, 99, 'Control node: check ansible version, Python version, SSH connectivity to managed nodes')))
    lines.append(R(bMid(3, 99, 'AWX: check service pods (Kubernetes), job queue depth, credential expiry dates')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Control Node Checks'), bMid(53, 99, 'AWX Checks'))))
    lines.append(R(merge(bMid(3, 50, 'ansible --version'), bMid(53, 99, 'kubectl get pods -n awx'))))
    lines.append(R(merge(bMid(3, 50, 'ansible all -m ping (all hosts)'), bMid(53, 99, 'AWX UI: Dashboard job stats'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-inventory --list --graph'), bMid(53, 99, 'awx jobs list --status failed'))))
    lines.append(R(merge(bMid(3, 50, 'Check Vault password accessible'), bMid(53, 99, 'Check credential expiry dates'))))
    lines.append(R(merge(bMid(3, 50, 'Verify EE images are current'), bMid(53, 99, 'AWX capacity: forks headroom'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ansible all -m ping   = fastest connectivity check; returns pong on success, unreachable on fail')))
    lines.append(R(bMid(3, 99, 'Job success rate      = AWX dashboard; alert if >5% failure rate over rolling 7 days')))
    lines.append(R(bMid(3, 99, 'EE freshness          = execution environment images; rebuild if base OS CVEs exist')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/install-upgrade', 'docs/automation/ansible/operations/install-upgrade/index.md', 'Ansible install and upgrade')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Install & Upgrade'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible core: install via pip (recommended) or OS package manager; pin version in requirements')))
    lines.append(R(bMid(3, 99, 'AWX: deployed on Kubernetes via the AWX Operator; upgrade by updating operator CRD version')))
    lines.append(R(bMid(3, 99, 'Collections: install via ansible-galaxy; pin in requirements.yml for reproducible environments')))
    lines.append(R(bMid(3, 99, 'Execution environments: built with ansible-builder; push to container registry; update AWX EE config')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Install Ansible Core'), bMid(53, 99, 'Upgrade AWX Operator'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -m venv ansible-env'), bMid(53, 99, '1. Edit kustomization.yaml'))))
    lines.append(R(merge(bMid(3, 50, 'pip install ansible-core==2.17'), bMid(53, 99, '2. Set newTag: <new-version>'))))
    lines.append(R(merge(bMid(3, 50, 'pip install -r requirements.txt'), bMid(53, 99, '3. kubectl apply -k .'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-galaxy install -r reqs'), bMid(53, 99, '4. Monitor pod rollout'))))
    lines.append(R(merge(bMid(3, 50, 'ansible --version (verify)'), bMid(53, 99, '5. Verify UI login, run test job'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ansible-builder = tool to create EE container images; reads execution-environment.yml')))
    lines.append(R(bMid(3, 99, 'AWX Operator    = Kubernetes operator managing AWX deployment lifecycle; CRD-driven config')))
    lines.append(R(bMid(3, 99, 'requirements.yml= collections/roles dependency file; ansible-galaxy install -r requirements.yml')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/procedures', 'docs/automation/ansible/operations/procedures/index.md', 'Ansible procedures')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Procedures'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Common Ansible operational procedures: rolling OS patching, credential rotation, inventory cleanup')))
    lines.append(R(bMid(3, 99, 'Pre-run: verify inventory is current, check mode first, confirm Vault password available')))
    lines.append(R(bMid(3, 99, 'Post-run: review job output for warnings, verify application health, update runbook with outcomes')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Rolling OS Patch Procedure'), bMid(53, 99, 'Credential Rotation'))))
    lines.append(R(merge(bMid(3, 50, '1. Run check mode: --check'), bMid(53, 99, '1. Generate new SSH key pair'))))
    lines.append(R(merge(bMid(3, 50, '2. Review changed tasks'), bMid(53, 99, '2. Add new pub key to hosts'))))
    lines.append(R(merge(bMid(3, 50, '3. Execute serial:1 (canary)'), bMid(53, 99, '3. Update AWX credential'))))
    lines.append(R(merge(bMid(3, 50, '4. Verify app health per host'), bMid(53, 99, '4. Remove old key from hosts'))))
    lines.append(R(merge(bMid(3, 50, '5. Increase serial, continue'), bMid(53, 99, '5. Verify connectivity'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'serial: 1     = run one host at a time; ensures app stays available during rolling ops')))
    lines.append(R(bMid(3, 99, 'max_fail_pct  = max_fail_percentage: 0 → abort if any host fails; safe for prod changes')))
    lines.append(R(bMid(3, 99, 'pre_tasks     = tasks that run before roles in a play; use for health checks and drain')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/operations/scripts', 'docs/automation/ansible/operations/scripts/index.md', 'Ansible scripts reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Scripts'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Utility scripts for Ansible operations: inventory validation, bulk vault re-key, job report export')))
    lines.append(R(bMid(3, 99, 'Scripts live in scripts/ at repo root; documented with usage header and example invocation')))
    lines.append(R(bMid(3, 99, 'AWX API scripts: list failed jobs, cancel stuck jobs, export all job templates to JSON')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Inventory Scripts'), bMid(53, 99, 'AWX API Scripts'))))
    lines.append(R(merge(bMid(3, 50, 'validate_inventory.py'), bMid(53, 99, 'list_failed_jobs.py'))))
    lines.append(R(merge(bMid(3, 50, 'compare_inventory.py'), bMid(53, 99, 'cancel_stuck_jobs.py'))))
    lines.append(R(merge(bMid(3, 50, 'generate_host_vars.py'), bMid(53, 99, 'export_job_templates.py'))))
    lines.append(R(merge(bMid(3, 50, 'prune_stale_hosts.py'), bMid(53, 99, 'rotate_credentials.py'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'AWX API     = REST API at /api/v2/; authenticate with bearer token; paginated JSON responses')))
    lines.append(R(bMid(3, 99, 'awx CLI     = official AWX CLI; wraps the REST API; install: pip install awxkit')))
    lines.append(R(bMid(3, 99, 'awxkit     = Python library for AWX API; used by the awx CLI; importable in scripts')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/security/index', 'docs/automation/ansible/security/index.md', 'Ansible security overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Security'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible security: protect credentials, restrict execution, audit changes, harden control node')))
    lines.append(R(bMid(3, 99, 'All secrets in Vault or AWX credential store — never plain text in playbooks or inventory')))
    lines.append(R(bMid(3, 99, 'RBAC in AWX: assign job templates to teams; prevent cross-team access to credentials')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Authentication'), bMid(36, 66, 'Access Control'), bMid(69, 99, 'Hardening'))))
    lines.append(R(merge(bMid(3, 33, 'SSH key per service'), bMid(36, 66, 'AWX teams + RBAC'), bMid(69, 99, 'Pin Ansible version'))))
    lines.append(R(merge(bMid(3, 33, 'Vault for secrets'), bMid(36, 66, 'No shared accounts'), bMid(69, 99, 'Audit task logs'))))
    lines.append(R(merge(bMid(3, 33, 'AWX credential store'), bMid(36, 66, 'Least privilege become'), bMid(69, 99, 'no_log on secrets'))))
    lines.append(R(merge(bMid(3, 33, 'Rotate keys regularly'), bMid(36, 66, 'Approval gates in AWX'), bMid(69, 99, 'MFA on AWX login'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible Vault = AES-256 encryption for variable files and strings; password stored separately')))
    lines.append(R(bMid(3, 99, 'AWX RBAC      = Role-Based Access Control; roles: admin, execute, read per job template/inventory')))
    lines.append(R(bMid(3, 99, 'no_log: true  = prevents task arguments and return values from appearing in AWX job logs')))
    lines.append(R(bMid(3, 99, 'become sudo   = privilege escalation; apply narrowly; log all sudo usage on managed nodes')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/security/access-control', 'docs/automation/ansible/security/access-control/index.md', 'Ansible access control')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Access Control'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible access control: who can run which playbooks against which hosts — enforced via AWX')))
    lines.append(R(bMid(3, 99, 'AWX RBAC model: organizations → teams → users; permissions per job template, inventory, credential')))
    lines.append(R(bMid(3, 99, 'CLI access: limit SSH key distribution; service accounts only; no shared personal keys')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'AWX RBAC Hierarchy'), bMid(53, 99, 'Permission Levels'))))
    lines.append(R(merge(bMid(3, 50, 'Organization → Team → User'), bMid(53, 99, 'Admin: full CRUD'))))
    lines.append(R(merge(bMid(3, 50, 'Credentials: team-scoped'), bMid(53, 99, 'Execute: run jobs only'))))
    lines.append(R(merge(bMid(3, 50, 'Inventories: team-scoped'), bMid(53, 99, 'Read: view job output'))))
    lines.append(R(merge(bMid(3, 50, 'Job templates: team-scoped'), bMid(53, 99, 'Use: attach to template'))))
    lines.append(R(merge(bMid(3, 50, 'LDAP/SAML sync for SSO'), bMid(53, 99, 'Approval: require review'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'AWX organization = top-level RBAC container; isolate business units or customers')))
    lines.append(R(bMid(3, 99, 'Team             = group of users within an organization; assign permissions at team level')))
    lines.append(R(bMid(3, 99, 'Approval workflow= AWX Workflow Job Template can require human approval step before execution')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/security/authentication', 'docs/automation/ansible/security/authentication/index.md', 'Ansible authentication')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Authentication'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible uses SSH key authentication for Linux targets and WinRM/Kerberos for Windows targets')))
    lines.append(R(bMid(3, 99, 'Service account per environment: ansible-prod, ansible-dev — no shared personal credentials')))
    lines.append(R(bMid(3, 99, 'AWX stores credentials encrypted (AES-256); injected at runtime — never written to disk on host')))
    lines.append(R(bMid(3, 99, 'AWX login: LDAP/AD or SAML SSO; local accounts only for break-glass scenarios')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Linux (SSH)'), bMid(53, 99, 'Windows (WinRM)'))))
    lines.append(R(merge(bMid(3, 50, 'SSH private key in AWX cred'), bMid(53, 99, 'Kerberos (domain-joined hosts)'))))
    lines.append(R(merge(bMid(3, 50, 'Service account on each host'), bMid(53, 99, 'NTLM fallback for workgroup'))))
    lines.append(R(merge(bMid(3, 50, 'Strict host key checking on'), bMid(53, 99, 'WinRM HTTPS (port 5986)'))))
    lines.append(R(merge(bMid(3, 50, 'Rotate annually or on staff change'), bMid(53, 99, 'ansible_winrm_transport: kerberos'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'WinRM          = Windows Remote Management; Microsoft implementation of WS-Management protocol')))
    lines.append(R(bMid(3, 99, 'Kerberos       = network auth protocol; used for Windows domain auth; requires domain membership')))
    lines.append(R(bMid(3, 99, 'SSH agent      = key agent; AWX injects private key into SSH agent process at job runtime')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/security/encryption', 'docs/automation/ansible/security/encryption/index.md', 'Ansible encryption')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Encryption'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible encryption layers: Vault (variable files), SSH transport (TLS), AWX credential encryption')))
    lines.append(R(bMid(3, 99, 'Ansible Vault: AES-256-CTR encryption; password protects vault files; vault ID for multiple PWs')))
    lines.append(R(bMid(3, 99, 'AWX: credentials encrypted at rest using SECRET_KEY; rotated during AWX rekey procedure')))
    lines.append(R(bMid(3, 99, 'Transport: SSH always encrypted; WinRM must use HTTPS (TLS) — reject HTTP WinRM in prod')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Vault Usage'), bMid(53, 99, 'AWX Credential Encryption'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-vault encrypt_string'), bMid(53, 99, 'Stored: AES-256 + SECRET_KEY'))))
    lines.append(R(merge(bMid(3, 50, 'Encrypt full var files'), bMid(53, 99, 'Injected at job runtime'))))
    lines.append(R(merge(bMid(3, 50, 'vault_id: prod, dev labels'), bMid(53, 99, 'Never logged or exposed'))))
    lines.append(R(merge(bMid(3, 50, 'Password in external vault/HSM'), bMid(53, 99, 'SECRET_KEY backup required'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'vault_id       = label for a vault password; allows different passwords for prod vs dev vaults')))
    lines.append(R(bMid(3, 99, 'SECRET_KEY     = AWX Django secret key; used to encrypt credentials in DB; MUST be backed up')))
    lines.append(R(bMid(3, 99, 'encrypt_string = encrypts a single string value; embed inline in plain YAML variable files')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/security/hardening', 'docs/automation/ansible/security/hardening/index.md', 'Ansible hardening')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Hardening'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Harden Ansible control node and AWX to reduce attack surface and prevent unauthorised automation')))
    lines.append(R(bMid(3, 99, 'Control node: restrict SSH access, pin package versions, run in isolated network segment')))
    lines.append(R(bMid(3, 99, 'AWX: HTTPS only, MFA, LDAP groups, session timeout, disable API browsable UI in prod')))
    lines.append(R(bMid(3, 99, 'Playbook hygiene: no_log on secrets, FQCN for all modules, lint before merge')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Control Node Hardening'), bMid(53, 99, 'AWX Hardening'))))
    lines.append(R(merge(bMid(3, 50, 'OS hardened baseline (CIS)'), bMid(53, 99, 'HTTPS only, valid cert'))))
    lines.append(R(merge(bMid(3, 50, 'SSH: AllowUsers ansible only'), bMid(53, 99, 'MFA + LDAP group mapping'))))
    lines.append(R(merge(bMid(3, 50, 'Pip packages pinned + audited'), bMid(53, 99, 'Session timeout: 30 min'))))
    lines.append(R(merge(bMid(3, 50, 'Outbound 22/5986 only'), bMid(53, 99, 'Disable local accounts (prod)'))))
    lines.append(R(merge(bMid(3, 50, 'ansible.cfg: host_key_checking=True'), bMid(53, 99, 'Kubernetes network policy'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'host_key_checking = True prevents connecting to hosts not in known_hosts; blocks MITM')))
    lines.append(R(bMid(3, 99, 'Network policy    = Kubernetes NetworkPolicy restricting AWX pod ingress/egress')))
    lines.append(R(bMid(3, 99, 'CIS baseline      = Centre for Internet Security hardening guide for the control node OS')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/troubleshooting/index', 'docs/automation/ansible/troubleshooting/index.md', 'Ansible troubleshooting overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Troubleshooting'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible troubleshooting: connectivity, variable resolution, module failures, AWX job issues')))
    lines.append(R(bMid(3, 99, 'First check: run with -vvv for full SSH debug output and module argument logging')))
    lines.append(R(bMid(3, 99, 'Common root causes: SSH key mismatch, Python missing on host, variable precedence error')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Common Issues'), bMid(53, 99, 'Diagnostic Commands'))))
    lines.append(R(merge(bMid(3, 50, 'UNREACHABLE: SSH timeout'), bMid(53, 99, 'ansible host -m ping -vvv'))))
    lines.append(R(merge(bMid(3, 50, 'FAILED: module not found'), bMid(53, 99, 'ansible-inventory --graph'))))
    lines.append(R(merge(bMid(3, 50, 'Wrong var value: precedence'), bMid(53, 99, 'ansible -m debug -a "var=x"'))))
    lines.append(R(merge(bMid(3, 50, 'Vault decrypt error'), bMid(53, 99, 'ansible-vault view file.yml'))))
    lines.append(R(merge(bMid(3, 50, 'AWX job stuck in pending'), bMid(53, 99, 'kubectl logs -n awx <pod>'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Variable precedence = 22 levels; host_vars > group_vars > defaults > role defaults (highest wins)')))
    lines.append(R(bMid(3, 99, 'UNREACHABLE        = host not reachable via SSH; check firewall, SSH service, key auth')))
    lines.append(R(bMid(3, 99, 'Python interpreter = set ansible_python_interpreter if /usr/bin/python3 differs on host')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/troubleshooting/common-issues', 'docs/automation/ansible/troubleshooting/common-issues/index.md', 'Ansible common issues')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Common Issues'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Most frequent Ansible failures: SSH connectivity, Python errors, Vault decrypt, module not found')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: UNREACHABLE — SSH connection refused or timed out')))
    lines.append(R(bMid(3, 99, 'Cause A: SSH service stopped on target → fix: start sshd on target')))
    lines.append(R(bMid(3, 99, 'Cause B: firewall blocking port 22 → fix: open port 22 in host/network FW')))
    lines.append(R(bMid(3, 99, 'Cause C: wrong ansible_host or ansible_port → fix: correct inventory variable')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Vault decryption failed')))
    lines.append(R(bMid(3, 99, 'Cause A: wrong password → fix: verify vault password; check vault_id label')))
    lines.append(R(bMid(3, 99, 'Cause B: wrong vault_id → fix: pass --vault-id prod@prompt if using vault IDs')))
    lines.append(R(bMid(3, 99, 'Cause C: AWX vault credential missing → fix: re-add vault credential to job template')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: module not found / collection missing')))
    lines.append(R(bMid(3, 99, 'Fix: ansible-galaxy collection install <namespace.collection> or add to requirements.yml')))
    lines.append(R(bMid(3, 99, 'Fix: rebuild EE image with collection included if running via AWX')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/troubleshooting/diagnostics', 'docs/automation/ansible/troubleshooting/diagnostics/index.md', 'Ansible diagnostics')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Diagnostics'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ansible diagnostic sequence: verify connectivity → check variables → inspect module output')))
    lines.append(R(bMid(3, 99, 'Increase verbosity progressively: -v (task results), -vv (input), -vvv (SSH debug), -vvvv')))
    lines.append(R(bMid(3, 99, 'AWX diagnostics: job event log, activity stream, kubectl logs for pod-level errors')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'CLI Diagnostics'), bMid(53, 99, 'AWX Diagnostics'))))
    lines.append(R(merge(bMid(3, 50, 'ansible host -m ping -vvv'), bMid(53, 99, 'Job → Event log tab'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-playbook --syntax-check'), bMid(53, 99, 'Settings → Activity stream'))))
    lines.append(R(merge(bMid(3, 50, 'ansible -m debug -a "var=x"'), bMid(53, 99, 'kubectl logs -n awx task'))))
    lines.append(R(merge(bMid(3, 50, 'ANSIBLE_DEBUG=1 ansible-play...'), bMid(53, 99, 'awx jobs stdout <id>'))))
    lines.append(R(merge(bMid(3, 50, 'ansible-inventory --list --yaml'), bMid(53, 99, 'AWX: Support bundle zip'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Activity stream    = AWX audit log; records every API call with user, timestamp, change detail')))
    lines.append(R(bMid(3, 99, 'ANSIBLE_DEBUG=1    = env var enabling maximum debug output from Ansible itself')))
    lines.append(R(bMid(3, 99, '--syntax-check     = parse playbook YAML without executing; catches syntax errors fast')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/ansible/troubleshooting/escalation', 'docs/automation/ansible/troubleshooting/escalation/index.md', 'Ansible escalation')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Ansible — Escalation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Escalate Ansible issues when: AWX pod crash-loops, Vault key lost, bulk playbook failures')))
    lines.append(R(bMid(3, 99, 'Tier 1: automation team (AWX job config, playbook bugs, inventory issues)')))
    lines.append(R(bMid(3, 99, 'Tier 2: platform/infra team (AWX Kubernetes deployment, network, storage)')))
    lines.append(R(bMid(3, 99, 'Tier 3: Red Hat support (AAP licensing, Ansible Core bugs, EE build failures)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Escalation Triggers'), bMid(53, 99, 'Info to Gather'))))
    lines.append(R(merge(bMid(3, 50, 'AWX pods crash-looping'), bMid(53, 99, 'AWX version, Ansible version'))))
    lines.append(R(merge(bMid(3, 50, 'Vault password lost'), bMid(53, 99, 'kubectl logs all AWX pods'))))
    lines.append(R(merge(bMid(3, 50, '>20% job failure rate'), bMid(53, 99, 'Job ID and full event log'))))
    lines.append(R(merge(bMid(3, 50, 'Performance: queue backing up'), bMid(53, 99, 'Inventory count, fork setting'))))
    lines.append(R(merge(bMid(3, 50, 'EE build broken repeatedly'), bMid(53, 99, 'ansible-builder log output'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Red Hat support  = access via access.redhat.com; requires active AAP subscription')))
    lines.append(R(bMid(3, 99, 'Must-gather      = AWX support bundle: Settings → Subscriptions → Download support bundle')))
    lines.append(R(bMid(3, 99, 'SLA              = AAP Premium: 1-hour response for Sev 1; Standard: 4 hours')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/index', 'docs/automation/github-actions/index.md', 'GitHub Actions overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — CI/CD Automation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions: native CI/CD platform; workflows defined in YAML under .github/workflows/')))
    lines.append(R(bMid(3, 99, 'Trigger: push, pull_request, schedule, workflow_dispatch, repository_dispatch, or webhooks')))
    lines.append(R(bMid(3, 99, 'Jobs run on runners: GitHub-hosted (ubuntu/windows/macos) or self-hosted (on-prem/cloud VMs)')))
    lines.append(R(bMid(3, 99, 'Actions marketplace: reusable steps; pin by SHA for security; audit before use')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Architecture'), bMid(36, 66, 'Operations'), bMid(69, 99, 'Security'))))
    lines.append(R(merge(bMid(3, 33, 'Workflow → job → step'), bMid(36, 66, 'Runner management'), bMid(69, 99, 'Secrets management'))))
    lines.append(R(merge(bMid(3, 33, 'Runner types'), bMid(36, 66, 'Monitoring job runs'), bMid(69, 99, 'OIDC cloud auth'))))
    lines.append(R(merge(bMid(3, 33, 'Environments + gates'), bMid(36, 66, 'Cache and artifacts'), bMid(69, 99, 'Branch protection'))))
    lines.append(R(merge(bMid(3, 33, 'Matrix strategy'), bMid(36, 66, 'Billing and minutes'), bMid(69, 99, 'Action pinning'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Workflow       = YAML file in .github/workflows/; defines triggers, jobs, and steps')))
    lines.append(R(bMid(3, 99, 'Job            = set of steps that run on a single runner; jobs run in parallel by default')))
    lines.append(R(bMid(3, 99, 'Step           = individual task in a job; uses: an action, or run: a shell command')))
    lines.append(R(bMid(3, 99, 'Action         = reusable unit of automation; published to GitHub Marketplace or repo-local')))
    lines.append(R(bMid(3, 99, 'Runner         = compute that executes jobs; GitHub-hosted or self-hosted')))
    lines.append(R(bMid(3, 99, 'Environment    = deployment target with protection rules, secrets, and required reviewers')))
    lines.append(R(bMid(3, 99, 'Matrix         = strategy to run a job across multiple variable combinations (OS, version)')))
    lines.append(R(bMid(3, 99, 'Artifact       = file uploaded from a job and available for download or subsequent jobs')))
    lines.append(R(bMid(3, 99, 'Cache          = store and restore dependencies across runs; keyed by hash of lock file')))
    lines.append(R(bMid(3, 99, 'OIDC           = OpenID Connect; GitHub Actions can request short-lived cloud tokens without secrets')))
    lines.append(R(bMid(3, 99, 'workflow_dispatch= manual trigger with optional input parameters; run from UI or API')))
    lines.append(R(bMid(3, 99, 'Concurrency    = group/cancel-in-progress setting to avoid duplicate runs on same branch')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/architecture/index', 'docs/automation/github-actions/architecture/index.md', 'GitHub Actions architecture overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Architecture'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions architecture: GitHub.com hosts the control plane; runners execute jobs')))
    lines.append(R(bMid(3, 99, 'Self-hosted runners poll GitHub Actions API over HTTPS — no inbound port required')))
    lines.append(R(bMid(3, 99, 'Runner groups: org or repo-level; assign self-hosted runners to specific workflows')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'How It Works'), bMid(53, 99, 'Design Standards'))))
    lines.append(R(merge(bMid(3, 50, 'Push → trigger → queue job'), bMid(53, 99, 'Pin actions to SHA'))))
    lines.append(R(merge(bMid(3, 50, 'Runner picks up job via poll'), bMid(53, 99, 'Minimal permissions in token'))))
    lines.append(R(merge(bMid(3, 50, 'Steps execute sequentially'), bMid(53, 99, 'Use environments for prod'))))
    lines.append(R(merge(bMid(3, 50, 'Results reported to GitHub UI'), bMid(53, 99, 'Cache deps, not secrets'))))
    lines.append(R(merge(bMid(3, 50, 'Logs streamed live to UI'), bMid(53, 99, 'Matrix over copy-paste jobs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical: GitHub-hosted runners are ephemeral VMs; self-hosted are VMs or bare-metal on-prem')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/architecture/how-it-works', 'docs/automation/github-actions/architecture/how-it-works/index.md', 'GitHub Actions how it works')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — How It Works'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Event fires → GitHub evaluates on: triggers → matching workflows queued → runner picks up job')))
    lines.append(R(bMid(3, 99, 'Runner clones repo, restores cache, executes steps, uploads artifacts, reports status')))
    lines.append(R(bMid(3, 99, 'Secrets injected as env vars at runtime; masked in log output automatically')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Trigger Phase'), bMid(36, 66, 'Execution Phase'), bMid(69, 99, 'Result Phase'))))
    lines.append(R(merge(bMid(3, 33, 'Event: push/PR/schedule'), bMid(36, 66, 'Runner accepts job'), bMid(69, 99, 'Status check posted'))))
    lines.append(R(merge(bMid(3, 33, 'on: filter evaluation'), bMid(36, 66, 'Repo checkout'), bMid(69, 99, 'Artifacts available'))))
    lines.append(R(merge(bMid(3, 33, 'Workflow file parsed'), bMid(36, 66, 'Cache restore'), bMid(69, 99, 'Logs retained 90d'))))
    lines.append(R(merge(bMid(3, 33, 'Jobs queued parallel'), bMid(36, 66, 'Steps run in order'), bMid(69, 99, 'Notify: PR, Slack'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GITHUB_TOKEN    = auto-generated per-job token; scoped to repo; expires when job ends')))
    lines.append(R(bMid(3, 99, 'permissions:    = restrict GITHUB_TOKEN scopes per job; principle of least privilege')))
    lines.append(R(bMid(3, 99, 'needs:          = declare job dependency; forces sequential execution and output passing')))
    lines.append(R(bMid(3, 99, 'if: condition   = conditional step/job execution; uses expression syntax ${{ }}')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/architecture/design-standards', 'docs/automation/github-actions/architecture/design-standards/index.md', 'GitHub Actions design standards')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Design Standards'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Standards for consistent, secure, maintainable GitHub Actions workflows across repos')))
    lines.append(R(bMid(3, 99, 'Security: pin all third-party actions by SHA; avoid GITHUB_TOKEN write-all permission')))
    lines.append(R(bMid(3, 99, 'Structure: one workflow per concern; reusable workflows for shared logic; no copy-paste')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Workflow Structure Rules'), bMid(53, 99, 'Security Rules'))))
    lines.append(R(merge(bMid(3, 50, 'Name workflows descriptively'), bMid(53, 99, 'Pin: uses: action@<sha>'))))
    lines.append(R(merge(bMid(3, 50, 'One workflow per concern'), bMid(53, 99, 'Minimal GITHUB_TOKEN perms'))))
    lines.append(R(merge(bMid(3, 50, 'Reusable workflow for shared CI'), bMid(53, 99, 'OIDC over static secrets'))))
    lines.append(R(merge(bMid(3, 50, 'Cache keyed on lock file hash'), bMid(53, 99, 'No secrets in env: at top level'))))
    lines.append(R(merge(bMid(3, 50, 'Concurrency: cancel-in-progress'), bMid(53, 99, 'Required reviewers for prod env'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Reusable workflow = workflow called via uses: org/repo/.github/workflows/shared.yml@main')))
    lines.append(R(bMid(3, 99, 'SHA pinning       = uses: actions/checkout@<40-char SHA> instead of @v4; prevents tag mutation')))
    lines.append(R(bMid(3, 99, 'OIDC              = workflow requests short-lived AWS/Azure token; no stored cloud credentials')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/architecture/integrations', 'docs/automation/github-actions/architecture/integrations/index.md', 'GitHub Actions integrations')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Integrations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions integrates with cloud providers, registries, ITSM, Slack, and security tools')))
    lines.append(R(bMid(3, 99, 'Cloud auth via OIDC: AWS, Azure, GCP — no stored access keys; federated identity')))
    lines.append(R(bMid(3, 99, 'Container registries: GHCR, ECR, ACR, Docker Hub — login action then docker push')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Cloud'), bMid(36, 66, 'DevOps Tools'), bMid(69, 99, 'Notifications'))))
    lines.append(R(merge(bMid(3, 33, 'AWS (OIDC → AssumeRole)'), bMid(36, 66, 'Terraform plan/apply'), bMid(69, 99, 'Slack webhook'))))
    lines.append(R(merge(bMid(3, 33, 'Azure (OIDC → federated)'), bMid(36, 66, 'Ansible AWX trigger'), bMid(69, 99, 'GitHub PR comment'))))
    lines.append(R(merge(bMid(3, 33, 'GCP (Workload Identity)'), bMid(36, 66, 'Docker build + push'), bMid(69, 99, 'Email on failure'))))
    lines.append(R(merge(bMid(3, 33, 'kubectl + kubeconfig'), bMid(36, 66, 'Security scan (Trivy)'), bMid(69, 99, 'PagerDuty alert'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'OIDC / Workload Identity = cloud-side trust policy allows GitHub Actions JWT to assume a role')))
    lines.append(R(bMid(3, 99, 'repository_dispatch      = external system triggers workflow via GitHub API POST event')))
    lines.append(R(bMid(3, 99, 'Deployment API           = GitHub Deployments track which SHA is deployed to which environment')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/index', 'docs/automation/github-actions/operations/index.md', 'GitHub Actions operations overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Operations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions operations: runner management, workflow monitoring, billing, secret rotation')))
    lines.append(R(bMid(3, 99, 'Self-hosted runner: install runner agent, register to org, manage as OS service')))
    lines.append(R(bMid(3, 99, 'Monitor: job queuing time, runner availability, billing minutes, failure rate per workflow')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Runner Operations'), bMid(53, 99, 'Workflow Operations'))))
    lines.append(R(merge(bMid(3, 50, 'Register self-hosted runner'), bMid(53, 99, 'Monitor job queue times'))))
    lines.append(R(merge(bMid(3, 50, 'Update runner agent software'), bMid(53, 99, 'Re-run failed jobs'))))
    lines.append(R(merge(bMid(3, 50, 'Scale runner pool size'), bMid(53, 99, 'Cancel stuck workflows'))))
    lines.append(R(merge(bMid(3, 50, 'Rotate runner tokens'), bMid(53, 99, 'Update pinned action SHAs'))))
    lines.append(R(merge(bMid(3, 50, 'Monitor runner disk/CPU'), bMid(53, 99, 'Review billing minutes'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Runner groups   = org-level grouping; restrict which repos can use specific self-hosted runners')))
    lines.append(R(bMid(3, 99, 'Actions Runner Controller (ARC) = Kubernetes operator for auto-scaling self-hosted runners')))
    lines.append(R(bMid(3, 99, 'Billing minutes = GitHub-hosted runner usage billed per minute; free tier: 2000 min/month')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/backup-restore', 'docs/automation/github-actions/operations/backup-restore/index.md', 'GitHub Actions backup and restore')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Backup & Restore'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions workflow state lives in the git repo — workflow YAML is the source of truth')))
    lines.append(R(bMid(3, 99, 'Secrets cannot be exported via API; document secret names and re-inject after repo migration')))
    lines.append(R(bMid(3, 99, 'Self-hosted runner: re-register after host rebuild; runner config stored in .runner file')))
    lines.append(R(bMid(3, 99, 'Environments: document protection rules (reviewers, wait timers) — export via GitHub API')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'What to Back Up'), bMid(53, 99, 'Restore Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Git repo (all workflow YAML)'), bMid(53, 99, '1. Restore git repo'))))
    lines.append(R(merge(bMid(3, 50, 'Secret names list (not values)'), bMid(53, 99, '2. Re-inject all secrets'))))
    lines.append(R(merge(bMid(3, 50, 'Environment config (API export)'), bMid(53, 99, '3. Re-create environments'))))
    lines.append(R(merge(bMid(3, 50, 'Self-hosted runner config docs'), bMid(53, 99, '4. Re-register runners'))))
    lines.append(R(merge(bMid(3, 50, 'Variable list and values'), bMid(53, 99, '5. Trigger test workflow'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub API      = REST API to export environments, protection rules, and variable names')))
    lines.append(R(bMid(3, 99, 'Variables       = non-sensitive config; visible in logs; exportable via API unlike secrets')))
    lines.append(R(bMid(3, 99, 'OIDC trust      = re-configure cloud OIDC trust relationship if repo moves orgs')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/cli-reference', 'docs/automation/github-actions/operations/cli-reference/index.md', 'GitHub Actions CLI reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — CLI Reference'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub CLI (gh) is the primary tool for interacting with GitHub Actions from the terminal')))
    lines.append(R(bMid(3, 99, 'Install: brew install gh or https://cli.github.com; authenticate: gh auth login')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Workflow Commands'), bMid(53, 99, 'Run Commands'))))
    lines.append(R(merge(bMid(3, 50, 'gh workflow list'), bMid(53, 99, 'gh run list'))))
    lines.append(R(merge(bMid(3, 50, 'gh workflow run <name>'), bMid(53, 99, 'gh run view <id>'))))
    lines.append(R(merge(bMid(3, 50, 'gh workflow enable/disable'), bMid(53, 99, 'gh run watch <id>'))))
    lines.append(R(merge(bMid(3, 50, 'gh workflow view <name>'), bMid(53, 99, 'gh run rerun <id>'))))
    lines.append(R(merge(bMid(3, 50, 'gh workflow run -f key=val'), bMid(53, 99, 'gh run cancel <id>'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Secret Commands'), bMid(53, 99, 'Runner Commands'))))
    lines.append(R(merge(bMid(3, 50, 'gh secret list'), bMid(53, 99, 'gh api /orgs/{org}/actions/runners'))))
    lines.append(R(merge(bMid(3, 50, 'gh secret set MY_SECRET'), bMid(53, 99, 'gh api DELETE runner/{id}'))))
    lines.append(R(merge(bMid(3, 50, 'gh secret delete MY_SECRET'), bMid(53, 99, 'config.sh --url --token'))))
    lines.append(R(merge(bMid(3, 50, 'gh variable list'), bMid(53, 99, './svc.sh install (as service)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'gh run watch = streams live log output to terminal; Ctrl-C detaches without cancelling run')))
    lines.append(R(bMid(3, 99, 'gh api       = raw API calls; use for endpoints not covered by gh subcommands')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/health-checks', 'docs/automation/github-actions/operations/health-checks/index.md', 'GitHub Actions health checks')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Health Checks'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Health checks for GitHub Actions: runner availability, job queue depth, workflow failure rate')))
    lines.append(R(bMid(3, 99, 'Monitor: Org Settings → Actions → Runners — check idle/active/offline count')))
    lines.append(R(bMid(3, 99, 'Alert conditions: self-hosted runner offline >5 min, queue depth >10, failure rate >10%')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Runner Health'), bMid(53, 99, 'Workflow Health'))))
    lines.append(R(merge(bMid(3, 50, 'gh api /orgs/{org}/runners'), bMid(53, 99, 'gh run list --status failure'))))
    lines.append(R(merge(bMid(3, 50, 'Runner status: online/offline'), bMid(53, 99, 'Workflow failure rate trend'))))
    lines.append(R(merge(bMid(3, 50, 'Runner version: check for updates'), bMid(53, 99, 'Queue wait time (Insights)'))))
    lines.append(R(merge(bMid(3, 50, 'Runner disk and CPU on host'), bMid(53, 99, 'Billing minutes consumed'))))
    lines.append(R(merge(bMid(3, 50, 'Runner labels match workflow'), bMid(53, 99, 'Secrets expiry dates'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Runner labels   = tags assigned to self-hosted runners; workflows select runner via runs-on:')))
    lines.append(R(bMid(3, 99, 'Insights tab    = repo/org-level: workflow run history, duration trends, billing breakdown')))
    lines.append(R(bMid(3, 99, 'Queue wait time = time from trigger to job start; high values = runner pool undersized')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/install-upgrade', 'docs/automation/github-actions/operations/install-upgrade/index.md', 'GitHub Actions install and upgrade')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Install & Upgrade'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions is a hosted service — no server-side installation; manage self-hosted runners')))
    lines.append(R(bMid(3, 99, 'Self-hosted runner install: download from GitHub, configure, register, run as OS service')))
    lines.append(R(bMid(3, 99, 'Runner updates: GitHub-hosted auto-update weekly; self-hosted manual or auto-update policy')))
    lines.append(R(bMid(3, 99, 'ARC (Actions Runner Controller): Kubernetes-based auto-scaling runner deployment')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Self-Hosted Runner Setup'), bMid(53, 99, 'ARC Setup (Kubernetes)'))))
    lines.append(R(merge(bMid(3, 50, '1. Settings → Actions → Runners'), bMid(53, 99, 'helm install arc oci://...'))))
    lines.append(R(merge(bMid(3, 50, '2. Download runner package'), bMid(53, 99, 'kubectl apply -f runnerdeployment'))))
    lines.append(R(merge(bMid(3, 50, '3. ./config.sh --url --token'), bMid(53, 99, 'Set minRunners/maxRunners'))))
    lines.append(R(merge(bMid(3, 50, '4. sudo ./svc.sh install'), bMid(53, 99, 'Configure runner labels'))))
    lines.append(R(merge(bMid(3, 50, '5. sudo ./svc.sh start'), bMid(53, 99, 'Verify runners register'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ARC             = Actions Runner Controller; Kubernetes operator managing ephemeral runner pods')))
    lines.append(R(bMid(3, 99, 'Runner version  = must be within 30 days of latest; GitHub stops routing jobs to old runners')))
    lines.append(R(bMid(3, 99, 'Ephemeral       = --ephemeral flag: runner deregisters after each job; clean environment per run')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/procedures', 'docs/automation/github-actions/operations/procedures/index.md', 'GitHub Actions procedures')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Procedures'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Common GitHub Actions procedures: secret rotation, runner re-registration, workflow migration')))
    lines.append(R(bMid(3, 99, 'Secret rotation: update secret value via gh CLI or UI; workflows pick up new value on next run')))
    lines.append(R(bMid(3, 99, 'Runner re-registration: remove stale runner, generate new token, re-run config.sh')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Secret Rotation'), bMid(53, 99, 'Runner Re-registration'))))
    lines.append(R(merge(bMid(3, 50, '1. Generate new credential'), bMid(53, 99, '1. Stop runner service'))))
    lines.append(R(merge(bMid(3, 50, '2. gh secret set NAME -b <val>'), bMid(53, 99, '2. ./svc.sh stop'))))
    lines.append(R(merge(bMid(3, 50, '3. Test workflow run'), bMid(53, 99, '3. ./config.sh remove --token'))))
    lines.append(R(merge(bMid(3, 50, '4. Revoke old credential'), bMid(53, 99, '4. Get new registration token'))))
    lines.append(R(merge(bMid(3, 50, '5. Document rotation date'), bMid(53, 99, '5. ./config.sh --url --token'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Registration token = short-lived (1 hour) token from GitHub used to register a new runner')))
    lines.append(R(bMid(3, 99, 'Remove token       = token used to cleanly deregister a runner from GitHub settings')))
    lines.append(R(bMid(3, 99, 'Workflow migration  = copy YAML to new repo; re-inject secrets; test before retiring old')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/operations/scripts', 'docs/automation/github-actions/operations/scripts/index.md', 'GitHub Actions scripts reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Scripts'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Utility scripts for GitHub Actions operations: bulk secret update, runner status report, audit')))
    lines.append(R(bMid(3, 99, 'Use gh CLI or GitHub REST API (PyGitHub / requests) for scripting GitHub Actions management')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Management Scripts'), bMid(53, 99, 'Audit Scripts'))))
    lines.append(R(merge(bMid(3, 50, 'bulk_set_secrets.py'), bMid(53, 99, 'audit_action_pins.py'))))
    lines.append(R(merge(bMid(3, 50, 'list_runner_status.py'), bMid(53, 99, 'check_workflow_permissions.py'))))
    lines.append(R(merge(bMid(3, 50, 'cancel_stuck_runs.py'), bMid(53, 99, 'report_billing_minutes.py'))))
    lines.append(R(merge(bMid(3, 50, 'migrate_secrets.py'), bMid(53, 99, 'find_unpinned_actions.py'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PyGitHub       = Python GitHub API client; pip install PyGithub; wraps REST and GraphQL')))
    lines.append(R(bMid(3, 99, 'gh api --paginate = fetch all pages of a paginated API response automatically')))
    lines.append(R(bMid(3, 99, 'Action audit   = scan all workflow YAML for uses: lines; verify each is pinned to a SHA')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/security/index', 'docs/automation/github-actions/security/index.md', 'GitHub Actions security overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Security'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions security: prevent secret exposure, supply chain attacks, privilege escalation')))
    lines.append(R(bMid(3, 99, 'Supply chain: pin all third-party actions by full SHA; review action source code before use')))
    lines.append(R(bMid(3, 99, 'Secrets: never echo; use ${{ secrets.X }} syntax; masked automatically in logs')))
    lines.append(R(bMid(3, 99, 'GITHUB_TOKEN: default permissions read-only; grant write only where explicitly needed')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Supply Chain'), bMid(36, 66, 'Secrets & Tokens'), bMid(69, 99, 'Runner Security'))))
    lines.append(R(merge(bMid(3, 33, 'Pin by SHA, not tag'), bMid(36, 66, 'OIDC over stored keys'), bMid(69, 99, 'Ephemeral runners'))))
    lines.append(R(merge(bMid(3, 33, 'Audit action updates'), bMid(36, 66, 'Minimal token perms'), bMid(69, 99, 'Isolated network'))))
    lines.append(R(merge(bMid(3, 33, 'Allowed action policy'), bMid(36, 66, 'No secrets in env:'), bMid(69, 99, 'No shared runners'))))
    lines.append(R(merge(bMid(3, 33, 'CodeQL for action scan'), bMid(36, 66, 'Rotate secrets quarterly'), bMid(69, 99, 'Label per environment'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Allowed actions policy = org setting restricting which actions can be used (own, verified, allowed list)')))
    lines.append(R(bMid(3, 99, 'Script injection       = PR from fork can inject code into ${{ github.event.pull_request.title }}')))
    lines.append(R(bMid(3, 99, 'pull_request_target    = runs with write perms even from forks — review carefully before using')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/security/access-control', 'docs/automation/github-actions/security/access-control/index.md', 'GitHub Actions access control')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Access Control'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions access: who can trigger workflows, approve deployments, manage secrets')))
    lines.append(R(bMid(3, 99, 'Repo permissions: read (view logs), write (trigger/cancel runs), admin (manage settings)')))
    lines.append(R(bMid(3, 99, 'Environment protection: required reviewers, wait timers, branch restrictions for deployments')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Workflow Permissions'), bMid(53, 99, 'Environment Gates'))))
    lines.append(R(merge(bMid(3, 50, 'GITHUB_TOKEN scoped per job'), bMid(53, 99, 'Required reviewers (team)'))))
    lines.append(R(merge(bMid(3, 50, 'permissions: read-all default'), bMid(53, 99, 'Wait timer: 10 min min'))))
    lines.append(R(merge(bMid(3, 50, 'Fork PRs: no secrets access'), bMid(53, 99, 'Branch restriction: main only'))))
    lines.append(R(merge(bMid(3, 50, 'workflow_dispatch: user auth'), bMid(53, 99, 'Deployment logs: auditable'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'CODEOWNERS     = file mapping code paths to review teams; auto-request review on PR')))
    lines.append(R(bMid(3, 99, 'Branch protection = require PR + CI pass + review before merge; enforces pipeline gate')))
    lines.append(R(bMid(3, 99, 'Deployment review = env requires named team approval; reviewer sees context and approves/rejects')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/security/authentication', 'docs/automation/github-actions/security/authentication/index.md', 'GitHub Actions authentication')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Authentication'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions authentication: GITHUB_TOKEN for GitHub API; OIDC for cloud; secrets for others')))
    lines.append(R(bMid(3, 99, 'OIDC preferred for AWS/Azure/GCP — no stored secrets; short-lived token per workflow run')))
    lines.append(R(bMid(3, 99, 'Service accounts: use GitHub Apps (fine-grained token) over PATs for org-wide access')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'GitHub Auth'), bMid(53, 99, 'Cloud Auth (OIDC)'))))
    lines.append(R(merge(bMid(3, 50, 'GITHUB_TOKEN: auto per job'), bMid(53, 99, 'aws-actions/configure-aws-creds'))))
    lines.append(R(merge(bMid(3, 50, 'GitHub App: installation token'), bMid(53, 99, 'role-to-assume: arn:aws:iam::...'))))
    lines.append(R(merge(bMid(3, 50, 'PAT: scoped personal token'), bMid(53, 99, 'Azure: azure/login@<sha>'))))
    lines.append(R(merge(bMid(3, 50, 'Deploy key: repo-level SSH key'), bMid(53, 99, 'GCP: auth.yml with workload id'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub App      = machine identity; fine-grained permissions; short-lived installation tokens')))
    lines.append(R(bMid(3, 99, 'PAT             = Personal Access Token; scoped to user; avoid for org automation (use App instead)')))
    lines.append(R(bMid(3, 99, 'OIDC token      = JWT issued by GitHub; cloud trusts issuer; exchanged for short-lived cloud cred')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/security/encryption', 'docs/automation/github-actions/security/encryption/index.md', 'GitHub Actions encryption')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Encryption'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions encrypts secrets at rest and in transit; secrets masked in all log output')))
    lines.append(R(bMid(3, 99, 'Secrets: stored encrypted in GitHub; decrypted only during job execution on runner')))
    lines.append(R(bMid(3, 99, 'Transport: all API calls and runner communication over TLS 1.2+')))
    lines.append(R(bMid(3, 99, 'Avoid: do not base64-encode secrets to work around masking — GitHub masks decoded too')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Secret Handling'), bMid(53, 99, 'Encryption Best Practices'))))
    lines.append(R(merge(bMid(3, 50, 'AES-256-GCM at rest (GitHub)'), bMid(53, 99, 'OIDC eliminates stored secrets'))))
    lines.append(R(merge(bMid(3, 50, 'Masked in all log output'), bMid(53, 99, 'Rotate secrets quarterly'))))
    lines.append(R(merge(bMid(3, 50, '${{ secrets.X }} injection'), bMid(53, 99, 'Env scope: job not step'))))
    lines.append(R(merge(bMid(3, 50, 'Never printed: echo $SECRET'), bMid(53, 99, 'Audit secret access in logs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Secret masking  = GitHub scans all log output and replaces secret values with ***')))
    lines.append(R(bMid(3, 99, 'add-mask        = ::add-mask::<value> step command; register dynamic values for masking')))
    lines.append(R(bMid(3, 99, 'Env scope       = set secret as env var at job level; available to all steps in that job')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/security/hardening', 'docs/automation/github-actions/security/hardening/index.md', 'GitHub Actions hardening')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Hardening'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Harden GitHub Actions against supply chain attacks, secret exposure, and privilege abuse')))
    lines.append(R(bMid(3, 99, 'Organisation settings: restrict allowed actions, require approval for first-time fork contributors')))
    lines.append(R(bMid(3, 99, 'Self-hosted runner: ephemeral mode, isolated network, minimal tools, no persistent state')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Org-Level Hardening'), bMid(53, 99, 'Runner Hardening'))))
    lines.append(R(merge(bMid(3, 50, 'Allowed actions: only verified'), bMid(53, 99, 'Ephemeral: --ephemeral flag'))))
    lines.append(R(merge(bMid(3, 50, 'Fork PR approval required'), bMid(53, 99, 'Network: egress allowlist'))))
    lines.append(R(merge(bMid(3, 50, 'Secrets: no org secrets in forks'), bMid(53, 99, 'No admin tools on runner'))))
    lines.append(R(merge(bMid(3, 50, 'Branch protection on default'), bMid(53, 99, 'Read-only filesystem where poss'))))
    lines.append(R(merge(bMid(3, 50, 'Audit log enabled + exported'), bMid(53, 99, 'Separate runner per environment'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Ephemeral runner = deregisters after each job; prevents state leakage between runs')))
    lines.append(R(bMid(3, 99, 'Egress allowlist = firewall rules on runner host limiting outbound to known endpoints')))
    lines.append(R(bMid(3, 99, 'Audit log        = org-level log of all Actions events; export to SIEM for long-term retention')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/troubleshooting/index', 'docs/automation/github-actions/troubleshooting/index.md', 'GitHub Actions troubleshooting overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Troubleshooting'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Common GitHub Actions issues: runner offline, job queued but not starting, secret not found')))
    lines.append(R(bMid(3, 99, 'Enable debug logging: ACTIONS_RUNNER_DEBUG=true and ACTIONS_STEP_DEBUG=true secrets')))
    lines.append(R(bMid(3, 99, 'gh run view --log <run-id> to fetch full log from CLI without opening the browser')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Common Issues'), bMid(53, 99, 'Diagnostic Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Job queued, no runner picks up'), bMid(53, 99, 'Check runner labels match'))))
    lines.append(R(merge(bMid(3, 50, 'Secret value empty in step'), bMid(53, 99, 'Set ACTIONS_STEP_DEBUG=true'))))
    lines.append(R(merge(bMid(3, 50, 'OIDC token request fails'), bMid(53, 99, 'Verify cloud trust policy'))))
    lines.append(R(merge(bMid(3, 50, 'Workflow not triggering on push'), bMid(53, 99, 'Check on: filter and branch'))))
    lines.append(R(merge(bMid(3, 50, 'Self-hosted runner offline'), bMid(53, 99, 'Check service: ./svc.sh status'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ACTIONS_STEP_DEBUG = secret value true; enables verbose per-step debug logging')))
    lines.append(R(bMid(3, 99, 'ACTIONS_RUNNER_DEBUG= runner-level debug; shows runner registration and job pickup detail')))
    lines.append(R(bMid(3, 99, 'Runner labels      = self-hosted runner must have all labels listed in runs-on: to pick up job')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/troubleshooting/common-issues', 'docs/automation/github-actions/troubleshooting/common-issues/index.md', 'GitHub Actions common issues')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Common Issues'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Most frequent GitHub Actions failures and their fixes')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Job queued but never picked up')))
    lines.append(R(bMid(3, 99, 'Cause A: no runner with matching labels → fix: add label to runner or change runs-on:')))
    lines.append(R(bMid(3, 99, 'Cause B: all runners busy → fix: scale runner pool or add GitHub-hosted runner fallback')))
    lines.append(R(bMid(3, 99, 'Cause C: runner offline → fix: sudo ./svc.sh start on runner host')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: OIDC token request fails')))
    lines.append(R(bMid(3, 99, 'Cause A: permissions: id-token: write missing → fix: add to workflow permissions:')))
    lines.append(R(bMid(3, 99, 'Cause B: cloud trust policy wrong → fix: verify sub claim matches workflow ref/repo')))
    lines.append(R(bMid(3, 99, 'Cause C: wrong region/audience → fix: check aws-actions configure-creds parameters')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Workflow not triggering on push')))
    lines.append(R(bMid(3, 99, 'Cause A: on: branches filter does not match current branch → fix: adjust filter')))
    lines.append(R(bMid(3, 99, 'Cause B: workflow file not on default branch → fix: merge to default branch first')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/troubleshooting/diagnostics', 'docs/automation/github-actions/troubleshooting/diagnostics/index.md', 'GitHub Actions diagnostics')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Diagnostics'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GitHub Actions diagnostic sequence: enable debug → inspect logs → check runner → verify secrets')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Debug Logging'), bMid(53, 99, 'Log Access'))))
    lines.append(R(merge(bMid(3, 50, 'Set ACTIONS_STEP_DEBUG=true'), bMid(53, 99, 'gh run view --log <id>'))))
    lines.append(R(merge(bMid(3, 50, 'Set ACTIONS_RUNNER_DEBUG=true'), bMid(53, 99, 'gh run view --log-failed <id>'))))
    lines.append(R(merge(bMid(3, 50, 'Re-run with debug enabled (UI)'), bMid(53, 99, 'Download log ZIP from UI'))))
    lines.append(R(merge(bMid(3, 50, 'Add: run: env (print env vars)'), bMid(53, 99, 'API: /repos/.../runs/{id}/logs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Debug secrets   = ACTIONS_STEP_DEBUG and ACTIONS_RUNNER_DEBUG are set as repo/env secrets')))
    lines.append(R(bMid(3, 99, 'Log retention   = 90 days default; configurable per repo; ZIP downloadable for archive')))
    lines.append(R(bMid(3, 99, 'run: env        = add as a step to print all environment variables; helps trace secret injection')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/github-actions/troubleshooting/escalation', 'docs/automation/github-actions/troubleshooting/escalation/index.md', 'GitHub Actions escalation')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'GitHub Actions — Escalation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Escalate GitHub Actions issues when: GitHub platform incident, persistent runner failures')))
    lines.append(R(bMid(3, 99, 'Check GitHub Status (githubstatus.com) before escalating — may be a platform incident')))
    lines.append(R(bMid(3, 99, 'Escalation path: dev team → platform/infra team → GitHub Enterprise support')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Escalation Triggers'), bMid(53, 99, 'Info to Gather'))))
    lines.append(R(merge(bMid(3, 50, 'GitHub platform incident (check status)'), bMid(53, 99, 'Run ID and full log'))))
    lines.append(R(merge(bMid(3, 50, 'Self-hosted runner persistent crash'), bMid(53, 99, 'Runner _diag/ log folder'))))
    lines.append(R(merge(bMid(3, 50, 'OIDC failing across all workflows'), bMid(53, 99, 'Cloud trust policy config'))))
    lines.append(R(merge(bMid(3, 50, 'Secret value corrupted / missing'), bMid(53, 99, 'GitHub audit log export'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'githubstatus.com = GitHub incident page; check first for platform-wide issues')))
    lines.append(R(bMid(3, 99, '_diag/ folder    = runner diagnostic logs; located in runner install directory')))
    lines.append(R(bMid(3, 99, 'GHE Support      = GitHub Enterprise support at support.github.com; requires Enterprise plan')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/index', 'docs/automation/powershell/index.md', 'PowerShell overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Shell and Scripting Automation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell: Microsoft shell and scripting language; cross-platform since PowerShell 7 (Core)')))
    lines.append(R(bMid(3, 99, 'Object pipeline: commands pass .NET objects, not text — enables structured data manipulation')))
    lines.append(R(bMid(3, 99, 'Modules: PSGallery ecosystem; key infra modules: Az, ActiveDirectory, VMware.PowerCLI, Dell')))
    lines.append(R(bMid(3, 99, 'PowerShell 5.1 = Windows-only (built-in); PowerShell 7+ = cross-platform (separate install)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Architecture'), bMid(36, 66, 'Operations'), bMid(69, 99, 'Security'))))
    lines.append(R(merge(bMid(3, 33, 'Object pipeline'), bMid(36, 66, 'Script management'), bMid(69, 99, 'Execution policy'))))
    lines.append(R(merge(bMid(3, 33, 'Modules + PSGallery'), bMid(36, 66, 'Remoting (WinRM)'), bMid(69, 99, 'Constrained lang'))))
    lines.append(R(merge(bMid(3, 33, 'DSC configuration'), bMid(36, 66, 'Scheduled tasks'), bMid(69, 99, 'JEA endpoints'))))
    lines.append(R(merge(bMid(3, 33, 'PS7 cross-platform'), bMid(36, 66, 'Error handling'), bMid(69, 99, 'AMSI integration'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell 5.1  = ships with Windows; cannot be updated separately; for Windows-only scripts')))
    lines.append(R(bMid(3, 99, 'PowerShell 7    = .NET Core-based; install separately; recommended for new scripts')))
    lines.append(R(bMid(3, 99, 'PSGallery       = Microsoft-hosted module repository; Install-Module / Install-PSResource')))
    lines.append(R(bMid(3, 99, 'DSC             = Desired State Configuration; declarative resource model for node config')))
    lines.append(R(bMid(3, 99, 'WinRM           = Windows Remote Management; enables Invoke-Command to remote hosts')))
    lines.append(R(bMid(3, 99, 'JEA             = Just Enough Administration; constrained endpoint for delegated admin')))
    lines.append(R(bMid(3, 99, 'AMSI            = Antimalware Scan Interface; Windows scans PS scripts before execution')))
    lines.append(R(bMid(3, 99, 'PowerCLI        = VMware PowerShell module for vSphere/vSAN/NSX management')))
    lines.append(R(bMid(3, 99, 'Az module       = Microsoft Azure PowerShell module; replacement for AzureRM')))
    lines.append(R(bMid(3, 99, 'CmdLet          = compiled command implementing Verb-Noun naming convention')))
    lines.append(R(bMid(3, 99, 'Pipeline        = | character passes output objects as input to next command')))
    lines.append(R(bMid(3, 99, 'ExecutionPolicy = Restricted/AllSigned/RemoteSigned/Unrestricted; per-scope setting')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/architecture/index', 'docs/automation/powershell/architecture/index.md', 'PowerShell architecture overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Architecture'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell architecture: runtime on .NET; cmdlets call .NET APIs; pipeline passes objects')))
    lines.append(R(bMid(3, 99, 'Remoting: Invoke-Command, Enter-PSSession → WinRM (Windows) or SSH transport (PS7/Linux)')))
    lines.append(R(bMid(3, 99, 'Modules: binary (compiled .dll) or script (.psm1); loaded from PSModulePath')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'How It Works'), bMid(53, 99, 'Design Standards'))))
    lines.append(R(merge(bMid(3, 50, 'Script → PSParser → AST → exec'), bMid(53, 99, 'Verb-Noun naming (approved verbs)'))))
    lines.append(R(merge(bMid(3, 50, 'Pipeline: object not string'), bMid(53, 99, 'CmdletBinding + param block'))))
    lines.append(R(merge(bMid(3, 50, 'Module autoload from path'), bMid(53, 99, 'Error handling: try/catch'))))
    lines.append(R(merge(bMid(3, 50, 'Runspaces: parallel execution'), bMid(53, 99, 'Pester tests for all functions'))))
    lines.append(R(merge(bMid(3, 50, 'Remoting: WinRM / SSH'), bMid(53, 99, 'PSScriptAnalyzer lint'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical: PS runs on Windows, Linux, macOS; remoting requires WinRM (Win) or SSH (PS7)')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/architecture/how-it-works', 'docs/automation/powershell/architecture/how-it-works/index.md', 'PowerShell how it works')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — How It Works'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell execution: script parsed to AST → pipeline stages → cmdlet execution → output')))
    lines.append(R(bMid(3, 99, 'Pipeline stages: BeginProcessing → ProcessRecord (per input object) → EndProcessing')))
    lines.append(R(bMid(3, 99, 'Error streams: terminating (throw/exception) vs non-terminating (Write-Error); $ErrorActionPreference')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Parse Phase'), bMid(36, 66, 'Execute Phase'), bMid(69, 99, 'Output Phase'))))
    lines.append(R(merge(bMid(3, 33, 'Script → tokens → AST'), bMid(36, 66, 'Cmdlet Begin()'), bMid(69, 99, 'Write-Output'))))
    lines.append(R(merge(bMid(3, 33, 'Syntax validation'), bMid(36, 66, 'Process() per object'), bMid(69, 99, 'Select-Object filter'))))
    lines.append(R(merge(bMid(3, 33, 'AMSI scan (Windows)'), bMid(36, 66, 'End() finalise'), bMid(69, 99, 'Format-* for display'))))
    lines.append(R(merge(bMid(3, 33, 'Execution policy check'), bMid(36, 66, 'Error stream handling'), bMid(69, 99, 'Export-Csv, ConvertTo'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'AST             = Abstract Syntax Tree; PS parses script before executing; enables static analysis')))
    lines.append(R(bMid(3, 99, '$ErrorActionPreference = Stop causes all errors to be terminating; catches via try/catch')))
    lines.append(R(bMid(3, 99, 'Runspace        = isolated execution context; enables parallel processing via Start-ThreadJob')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/architecture/design-standards', 'docs/automation/powershell/architecture/design-standards/index.md', 'PowerShell design standards')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Design Standards'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell standards: approved verbs, CmdletBinding, proper error handling, Pester tests')))
    lines.append(R(bMid(3, 99, 'Use PSScriptAnalyzer in CI; pin module versions with #Requires or RequiredModules in manifest')))
    lines.append(R(bMid(3, 99, 'All scripts require help block: .SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Script Structure'), bMid(53, 99, 'Error Handling'))))
    lines.append(R(merge(bMid(3, 50, '[CmdletBinding(SupportsShouldProcess)]'), bMid(53, 99, '$ErrorActionPreference = Stop'))))
    lines.append(R(merge(bMid(3, 50, 'param() block with types'), bMid(53, 99, 'try { } catch { } finally {}'))))
    lines.append(R(merge(bMid(3, 50, 'begin/process/end blocks'), bMid(53, 99, 'Write-Error -ErrorRecord'))))
    lines.append(R(merge(bMid(3, 50, 'comment-based help block'), bMid(53, 99, 'Throw for terminating errors'))))
    lines.append(R(merge(bMid(3, 50, '#Requires -Version 7.0'), bMid(53, 99, 'Log with Write-Verbose'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'SupportsShouldProcess = enables -WhatIf and -Confirm; use $PSCmdlet.ShouldProcess() guard')))
    lines.append(R(bMid(3, 99, 'PSScriptAnalyzer      = static analysis; run in CI with Invoke-ScriptAnalyzer -Severity Error')))
    lines.append(R(bMid(3, 99, 'Pester                = PS testing framework; Describe/Context/It blocks; mock with Mock{}')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/architecture/integrations', 'docs/automation/powershell/architecture/integrations/index.md', 'PowerShell integrations')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Integrations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell integrates with Microsoft ecosystem and infrastructure platforms via modules')))
    lines.append(R(bMid(3, 99, 'Key modules: Az (Azure), ActiveDirectory, VMware.PowerCLI, DellEMC.OpenManage, ImportExcel')))
    lines.append(R(bMid(3, 99, 'REST API integration via Invoke-RestMethod; parses JSON/XML responses to PS objects')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Microsoft Platform'), bMid(36, 66, 'Infra Platform'), bMid(69, 99, 'Automation'))))
    lines.append(R(merge(bMid(3, 33, 'Az: Azure management'), bMid(36, 66, 'VMware.PowerCLI'), bMid(69, 99, 'GitHub Actions runner'))))
    lines.append(R(merge(bMid(3, 33, 'ActiveDirectory module'), bMid(36, 66, 'Dell OpenManage'), bMid(69, 99, 'Ansible WinRM target'))))
    lines.append(R(merge(bMid(3, 33, 'ExchangeOnlineManagement'), bMid(36, 66, 'NetApp PowerShell'), bMid(69, 99, 'Scheduled Task (schtasks)'))))
    lines.append(R(merge(bMid(3, 33, 'Microsoft.Graph module'), bMid(36, 66, 'Rubrik Security Cloud'), bMid(69, 99, 'PS Remoting (WinRM/SSH)'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Invoke-RestMethod = HTTP client; returns PS objects from JSON APIs; handles auth headers')))
    lines.append(R(bMid(3, 99, 'PowerCLI          = VMware.PowerCLI; Connect-VIServer then Get-VM, Start-VM, etc.')))
    lines.append(R(bMid(3, 99, 'Az module         = Connect-AzAccount; then Get-AzVM, New-AzResourceGroup, etc.')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/index', 'docs/automation/powershell/operations/index.md', 'PowerShell operations overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Operations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell operations: script deployment, module updates, remoting configuration, DSC management')))
    lines.append(R(bMid(3, 99, 'Module management: Install-Module, Update-Module, Get-InstalledModule; use -Scope AllUsers for shared')))
    lines.append(R(bMid(3, 99, 'Remoting: Enable-PSRemoting; configure WinRM HTTPS; test with Test-WSMan')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Module Operations'), bMid(53, 99, 'Remoting Operations'))))
    lines.append(R(merge(bMid(3, 50, 'Find-Module <name>'), bMid(53, 99, 'Enable-PSRemoting -Force'))))
    lines.append(R(merge(bMid(3, 50, 'Install-Module -Name <x> -Force'), bMid(53, 99, 'Test-WSMan <hostname>'))))
    lines.append(R(merge(bMid(3, 50, 'Update-Module -Name <x>'), bMid(53, 99, 'Enter-PSSession <host>'))))
    lines.append(R(merge(bMid(3, 50, 'Get-InstalledModule | sort'), bMid(53, 99, 'Invoke-Command -Computer'))))
    lines.append(R(merge(bMid(3, 50, 'Uninstall-Module -AllVersions'), bMid(53, 99, 'New-PSSession (persistent)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Enable-PSRemoting = configures WinRM listener and firewall rule; required before remoting works')))
    lines.append(R(bMid(3, 99, 'PSRepository      = Register-PSRepository to add internal feed (Nexus, Artifactory, ProGet)')))
    lines.append(R(bMid(3, 99, '-AllowClobber     = Install-Module flag; overrides existing conflicting commands from other modules')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/backup-restore', 'docs/automation/powershell/operations/backup-restore/index.md', 'PowerShell backup and restore')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Backup & Restore'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell script backup: store all .ps1/.psm1/.psd1 in git — git is the source of truth')))
    lines.append(R(bMid(3, 99, 'DSC configurations: check in to git; MOF files are generated from config — do not store MOF')))
    lines.append(R(bMid(3, 99, 'Restore: clone repo, install pinned module versions, re-configure remoting endpoints')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'What to Back Up'), bMid(53, 99, 'Restore Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Git repo (all .ps1, .psm1, .psd1)'), bMid(53, 99, '1. Clone repo to new host'))))
    lines.append(R(merge(bMid(3, 50, 'Module version list (lock file)'), bMid(53, 99, '2. Install pinned modules'))))
    lines.append(R(merge(bMid(3, 50, 'JEA endpoint configs'), bMid(53, 99, '3. Register PSRepository'))))
    lines.append(R(merge(bMid(3, 50, 'Scheduled task definitions'), bMid(53, 99, '4. Restore JEA endpoints'))))
    lines.append(R(merge(bMid(3, 50, 'PSRepository registrations'), bMid(53, 99, '5. Verify test script runs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Module lock    = document exact versions: Get-InstalledModule | Export-Csv modules.csv')))
    lines.append(R(bMid(3, 99, 'JEA config     = session configuration .pssc file; register with Register-PSSessionConfiguration')))
    lines.append(R(bMid(3, 99, 'Scheduled task = Export-ScheduledTask | Out-File; Import via Register-ScheduledTask')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/cli-reference', 'docs/automation/powershell/operations/cli-reference/index.md', 'PowerShell CLI reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — CLI Reference'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Essential PowerShell commands for daily infrastructure operations')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Core Commands'), bMid(53, 99, 'Utility Commands'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Command -Module <name>'), bMid(53, 99, 'Measure-Object -Sum'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Help <cmd> -Full'), bMid(53, 99, 'Where-Object { $_.X -eq Y }'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Member (alias: gm)'), bMid(53, 99, 'Select-Object -First 10'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Module -ListAvailable'), bMid(53, 99, 'Sort-Object -Property Name'))))
    lines.append(R(merge(bMid(3, 50, 'Import-Module <name>'), bMid(53, 99, 'Group-Object -Property Type'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'File and Data'), bMid(53, 99, 'Remote Execution'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Content, Set-Content'), bMid(53, 99, 'Invoke-Command -ScriptBlock'))))
    lines.append(R(merge(bMid(3, 50, 'Import-Csv, Export-Csv'), bMid(53, 99, 'Enter-PSSession <host>'))))
    lines.append(R(merge(bMid(3, 50, 'ConvertTo-Json, ConvertFrom-Json'), bMid(53, 99, 'Copy-Item -ToSession $sess'))))
    lines.append(R(merge(bMid(3, 50, 'Invoke-RestMethod (API calls)'), bMid(53, 99, 'Disconnect-PSSession'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Get-Member     = inspect object properties and methods; essential for pipeline debugging')))
    lines.append(R(bMid(3, 99, 'ForEach-Object = pipeline loop; alias: %; $_  is current object; use for per-item processing')))
    lines.append(R(bMid(3, 99, 'Where-Object   = filter pipeline; alias: ?; $_.Property -eq "value"')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/health-checks', 'docs/automation/powershell/operations/health-checks/index.md', 'PowerShell health checks')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Health Checks'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell health checks: verify PS version, remoting, module currency, and script execution')))
    lines.append(R(bMid(3, 99, 'Check: $PSVersionTable.PSVersion, Test-WSMan, Get-PSRepository, PSScriptAnalyzer run')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Version and Environment'), bMid(53, 99, 'Remoting Health'))))
    lines.append(R(merge(bMid(3, 50, '$PSVersionTable.PSVersion'), bMid(53, 99, 'Test-WSMan <hostname>'))))
    lines.append(R(merge(bMid(3, 50, 'Get-Module -ListAvailable'), bMid(53, 99, 'Test-NetConnection -port 5985'))))
    lines.append(R(merge(bMid(3, 50, 'Get-InstalledModule (PSGallery)'), bMid(53, 99, 'Get-PSSessionConfiguration'))))
    lines.append(R(merge(bMid(3, 50, 'Get-ExecutionPolicy -List'), bMid(53, 99, 'winrm get winrm/config'))))
    lines.append(R(merge(bMid(3, 50, 'Invoke-ScriptAnalyzer -Path .'), bMid(53, 99, 'Check JEA endpoints active'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, '$PSVersionTable = hash table with PS version, OS, runtime, and build info')))
    lines.append(R(bMid(3, 99, 'Test-WSMan      = validates WinRM connectivity and config on remote host; returns XML')))
    lines.append(R(bMid(3, 99, 'ExecutionPolicy = check per scope: Process, CurrentUser, LocalMachine; Restricted blocks all .ps1')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/install-upgrade', 'docs/automation/powershell/operations/install-upgrade/index.md', 'PowerShell install and upgrade')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Install & Upgrade'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell 7 install: MSI (Windows), package manager (brew, apt, yum), or GitHub releases')))
    lines.append(R(bMid(3, 99, 'Upgrade: download new MSI and run; side-by-side with PS 5.1 on Windows; no conflict')))
    lines.append(R(bMid(3, 99, 'Module updates: Update-Module -Force; or pin specific version with -RequiredVersion')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Windows Install'), bMid(53, 99, 'Linux Install'))))
    lines.append(R(merge(bMid(3, 50, 'winget install Microsoft.PowerShell'), bMid(53, 99, 'snap install powershell (Ubuntu)'))))
    lines.append(R(merge(bMid(3, 50, 'Or: download MSI from GitHub'), bMid(53, 99, 'brew install powershell (macOS)'))))
    lines.append(R(merge(bMid(3, 50, '$PSVersionTable (verify version)'), bMid(53, 99, 'pwsh (launch after install)'))))
    lines.append(R(merge(bMid(3, 50, 'Update: winget upgrade PS'), bMid(53, 99, 'apt upgrade powershell'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Side-by-side = PS 7 and 5.1 coexist on Windows; PS 7 binary: pwsh.exe, 5.1: powershell.exe')))
    lines.append(R(bMid(3, 99, 'Profile       = $PROFILE; per-user or all-users startup script; loads modules, aliases')))
    lines.append(R(bMid(3, 99, 'NuGet provider= required for PSGallery; Install-PackageProvider -Name NuGet if missing')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/procedures', 'docs/automation/powershell/operations/procedures/index.md', 'PowerShell procedures')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Procedures'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Common PowerShell operational procedures: bulk host operations, AD queries, module updates')))
    lines.append(R(bMid(3, 99, 'Bulk remoting: Invoke-Command -ComputerName $servers -ScriptBlock {} -ThrottleLimit 20')))
    lines.append(R(bMid(3, 99, 'Pre-flight: test remoting to all targets; use -WhatIf on destructive commands first')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Bulk Remote Operations'), bMid(53, 99, 'Module Update Procedure'))))
    lines.append(R(merge(bMid(3, 50, '$servers = Get-ADComputer -Filter *'), bMid(53, 99, '1. List installed: Get-InstalledModule'))))
    lines.append(R(merge(bMid(3, 50, 'Invoke-Command -ComputerName'), bMid(53, 99, '2. Check updates: Find-Module'))))
    lines.append(R(merge(bMid(3, 50, '  $servers.Name -ScriptBlock {}'), bMid(53, 99, '3. Test in dev: Update-Module'))))
    lines.append(R(merge(bMid(3, 50, '-ThrottleLimit 20 (parallel cap)'), bMid(53, 99, '4. Run scripts: verify output'))))
    lines.append(R(merge(bMid(3, 50, '$results | Export-Csv output.csv'), bMid(53, 99, '5. Deploy to prod hosts'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, '-ThrottleLimit = max concurrent remote sessions in Invoke-Command; default 32; reduce for stability')))
    lines.append(R(bMid(3, 99, '-WhatIf        = simulates what would happen; use before Remove-Item, Stop-Service, etc.')))
    lines.append(R(bMid(3, 99, 'Transcript     = Start-Transcript; logs all input/output to file; use for audit trail')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/operations/scripts', 'docs/automation/powershell/operations/scripts/index.md', 'PowerShell scripts reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Scripts (Operations)'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Operational PowerShell scripts for common infrastructure management tasks')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Windows Admin Scripts'), bMid(53, 99, 'Reporting Scripts'))))
    lines.append(R(merge(bMid(3, 50, 'Get-DiskHealth.ps1'), bMid(53, 99, 'Get-ServerInventory.ps1'))))
    lines.append(R(merge(bMid(3, 50, 'Set-LocalAdminPassword.ps1'), bMid(53, 99, 'Get-EventLogErrors.ps1'))))
    lines.append(R(merge(bMid(3, 50, 'Enable-WinRMHTTPS.ps1'), bMid(53, 99, 'Export-ADUserReport.ps1'))))
    lines.append(R(merge(bMid(3, 50, 'Install-RequiredModules.ps1'), bMid(53, 99, 'Check-CertExpiry.ps1'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Script header = always include #Requires, help block, param block with ValidateSet/Mandatory')))
    lines.append(R(bMid(3, 99, 'Logging       = use Write-Verbose for debug; Start-Transcript for full session log')))
    lines.append(R(bMid(3, 99, 'Return values = output objects not strings; allows caller to filter with Where-Object')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/scripts', 'docs/automation/powershell/scripts/index.md', 'PowerShell scripts library')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Scripts Library'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell scripts library: reusable scripts for Active Directory, Azure, VMware, and storage')))
    lines.append(R(bMid(3, 99, 'Organised by platform: scripts/ad/, scripts/azure/, scripts/vmware/, scripts/storage/')))
    lines.append(R(bMid(3, 99, 'All scripts: version header, help block, error handling, Pester test counterpart')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Active Directory'), bMid(36, 66, 'Azure / Cloud'), bMid(69, 99, 'Infrastructure'))))
    lines.append(R(merge(bMid(3, 33, 'New-ADUser-Bulk.ps1'), bMid(36, 66, 'Get-AzVMReport.ps1'), bMid(69, 99, 'Get-VMwareVMs.ps1'))))
    lines.append(R(merge(bMid(3, 33, 'Disable-StaleAccounts.ps1'), bMid(36, 66, 'Set-AzTagsBulk.ps1'), bMid(69, 99, 'Get-DellHWAlert.ps1'))))
    lines.append(R(merge(bMid(3, 33, 'Get-ADGroupMembers.ps1'), bMid(36, 66, 'New-AzResourceGroup.ps1'), bMid(69, 99, 'Test-SANConnectivity.ps1'))))
    lines.append(R(merge(bMid(3, 33, 'Sync-ADAttributes.ps1'), bMid(36, 66, 'Export-AzCostReport.ps1'), bMid(69, 99, 'Invoke-VeeamReport.ps1'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Script versioning = use semantic version in script header; update on every meaningful change')))
    lines.append(R(bMid(3, 99, 'Pester test       = scripts/tests/<ScriptName>.Tests.ps1; run: Invoke-Pester -Path tests/')))
    lines.append(R(bMid(3, 99, 'Code review       = all new scripts reviewed via pull request before merge to main')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/security/index', 'docs/automation/powershell/security/index.md', 'PowerShell security overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Security'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell security: execution policy, code signing, AMSI integration, script block logging')))
    lines.append(R(bMid(3, 99, 'Enable: ScriptBlockLogging + ModuleLogging + Protected Event Logging in Group Policy')))
    lines.append(R(bMid(3, 99, 'JEA: Just Enough Administration — constrained PS endpoints with minimal allowed commands')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Execution Controls'), bMid(36, 66, 'Logging'), bMid(69, 99, 'Delegation'))))
    lines.append(R(merge(bMid(3, 33, 'ExecutionPolicy: AllSigned'), bMid(36, 66, 'ScriptBlock logging'), bMid(69, 99, 'JEA endpoints'))))
    lines.append(R(merge(bMid(3, 33, 'Code signing (cert)'), bMid(36, 66, 'Module logging'), bMid(69, 99, 'RBAC via JEA'))))
    lines.append(R(merge(bMid(3, 33, 'AMSI: malware scan'), bMid(36, 66, 'Protected Event Log'), bMid(69, 99, 'Constrained lang mode'))))
    lines.append(R(merge(bMid(3, 33, 'Constrained lang mode'), bMid(36, 66, 'SIEM forwarding'), bMid(69, 99, 'No interactive admin'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ScriptBlock logging = logs every command block executed; enables forensics post-incident')))
    lines.append(R(bMid(3, 99, 'Constrained lang   = restricts allowed .NET types in PS; limits attacker capabilities')))
    lines.append(R(bMid(3, 99, 'JEA                = session config with VisibleCmdlets list; operators use PS without admin')))
    lines.append(R(bMid(3, 99, 'Protected Event Log= encrypts event log entries so only authorised users can decrypt')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/security/access-control', 'docs/automation/powershell/security/access-control/index.md', 'PowerShell access control')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Access Control'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell access control: who can run scripts, remoting access, JEA capability delegation')))
    lines.append(R(bMid(3, 99, 'WinRM access: WS-Management ACL; restrict to security groups, not all users')))
    lines.append(R(bMid(3, 99, 'JEA: define role capabilities (.psrc); register session config (.pssc); assign via AD groups')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'WinRM Access Control'), bMid(53, 99, 'JEA Configuration'))))
    lines.append(R(merge(bMid(3, 50, 'Set-PSSessionConfiguration DACL'), bMid(53, 99, 'New-PSRoleCapabilityFile'))))
    lines.append(R(merge(bMid(3, 50, 'Grant to AD group only'), bMid(53, 99, 'New-PSSessionConfigurationFile'))))
    lines.append(R(merge(bMid(3, 50, 'Deny interactive logon to SVC'), bMid(53, 99, 'Register-PSSessionConfiguration'))))
    lines.append(R(merge(bMid(3, 50, 'HTTPS WinRM: port 5986 only'), bMid(53, 99, 'Test-PSSessionConfigurationFile'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'DACL         = Discretionary ACL on PS session config; controls which identities can connect')))
    lines.append(R(bMid(3, 99, '.psrc file   = Role Capability file; defines VisibleCmdlets, VisibleFunctions, VisibleProviders')))
    lines.append(R(bMid(3, 99, '.pssc file   = Session Configuration file; maps AD groups to role capability files')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/security/authentication', 'docs/automation/powershell/security/authentication/index.md', 'PowerShell authentication')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Authentication'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell authentication: Kerberos (domain), NTLM (fallback), certificate-based for remoting')))
    lines.append(R(bMid(3, 99, 'Service principal: use certificate auth for Azure/cloud (no password rotation needed)')))
    lines.append(R(bMid(3, 99, 'Secrets in scripts: use SecretManagement module; back-ends: KeyVault, SecretStore, CyberArk')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Remoting Auth'), bMid(53, 99, 'Secret Management'))))
    lines.append(R(merge(bMid(3, 50, 'Kerberos: domain joined default'), bMid(53, 99, 'Install-Module SecretManagement'))))
    lines.append(R(merge(bMid(3, 50, 'NTLM: workgroup or cross-domain'), bMid(53, 99, 'Register-SecretVault -Name AKV'))))
    lines.append(R(merge(bMid(3, 50, 'Certificate: mutual TLS WinRM'), bMid(53, 99, 'Get-Secret -Name MyPassword'))))
    lines.append(R(merge(bMid(3, 50, 'CredSSP: avoid (double-hop only)'), bMid(53, 99, 'No ConvertTo-SecureString -Key'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'SecretManagement = PS module; abstraction layer for secret retrieval; vault-agnostic')))
    lines.append(R(bMid(3, 99, 'CredSSP          = delegates credentials to remote host; security risk; use Kerberos or JEA')))
    lines.append(R(bMid(3, 99, 'Managed Identity = Azure-side auth; PS running in Azure VM can get token without credentials')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/security/encryption', 'docs/automation/powershell/security/encryption/index.md', 'PowerShell encryption')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Encryption'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell encryption: SecureString for in-memory, DPAPI for user-scope, SecretManagement for vaults')))
    lines.append(R(bMid(3, 99, 'Transport: WinRM HTTPS (TLS 1.2+); enforce: winrm set winrm/config/listener @{Transport=HTTPS}')))
    lines.append(R(bMid(3, 99, 'Avoid: ConvertTo-SecureString with -Key flag stores key alongside data — use vault instead')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Secret Handling'), bMid(53, 99, 'Transport Security'))))
    lines.append(R(merge(bMid(3, 50, 'SecureString: in-memory only'), bMid(53, 99, 'WinRM HTTPS: port 5986'))))
    lines.append(R(merge(bMid(3, 50, 'DPAPI: current user scope'), bMid(53, 99, 'TLS 1.2+ enforced'))))
    lines.append(R(merge(bMid(3, 50, 'SecretManagement: AKV / KeePass'), bMid(53, 99, 'Certificate required on host'))))
    lines.append(R(merge(bMid(3, 50, 'No plain-text passwords in .ps1'), bMid(53, 99, 'Disable HTTP WinRM (port 5985)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'DPAPI           = Windows Data Protection API; encrypts to current user or machine scope')))
    lines.append(R(bMid(3, 99, 'SecureString    = in-memory encrypted string; cannot be stored safely to disk without DPAPI')))
    lines.append(R(bMid(3, 99, 'Azure Key Vault = recommended back-end for SecretManagement in cloud/hybrid environments')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/security/hardening', 'docs/automation/powershell/security/hardening/index.md', 'PowerShell hardening')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Hardening'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell hardening: logging, execution control, JEA, AMSI, code signing — deploy via GPO')))
    lines.append(R(bMid(3, 99, 'Enable via Group Policy: ScriptBlockLogging, ModuleLogging, TranscriptLogging')))
    lines.append(R(bMid(3, 99, 'Set ExecutionPolicy AllSigned at machine scope; sign all production scripts with code cert')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'GPO Settings (Apply via GPO)'), bMid(53, 99, 'Host Hardening'))))
    lines.append(R(merge(bMid(3, 50, 'ScriptBlockLogging: Enabled'), bMid(53, 99, 'ExecutionPolicy: AllSigned'))))
    lines.append(R(merge(bMid(3, 50, 'ModuleLogging: Enabled'), bMid(53, 99, 'Constrained Language Mode'))))
    lines.append(R(merge(bMid(3, 50, 'Transcription: Enabled + path'), bMid(53, 99, 'Disable PS 2.0 (no logging)'))))
    lines.append(R(merge(bMid(3, 50, 'ProtectedEventLogging: Enabled'), bMid(53, 99, 'Remove PS v2 Windows feature'))))
    lines.append(R(merge(bMid(3, 50, 'HTTPS WinRM: enforce'), bMid(53, 99, 'AMSI: do not disable'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PS v2 removal  = Disable-WindowsOptionalFeature -FeatureName MicrosoftWindowsPowerShellV2')))
    lines.append(R(bMid(3, 99, 'Code signing   = Get-AuthenticodeSignature; sign with: Set-AuthenticodeSignature -Cert $cert')))
    lines.append(R(bMid(3, 99, 'AMSI bypass    = attackers attempt to disable; monitor for AMSI-related events in security logs')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/troubleshooting/index', 'docs/automation/powershell/troubleshooting/index.md', 'PowerShell troubleshooting overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Troubleshooting'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell troubleshooting: execution errors, remoting failures, module issues, DSC drift')))
    lines.append(R(bMid(3, 99, 'Enable verbose output: $VerbosePreference = "Continue"; add -Verbose flag to commands')))
    lines.append(R(bMid(3, 99, 'Check $Error[0] for last error; $Error[0].InnerException for root cause')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Common Issues'), bMid(53, 99, 'Diagnostic Commands'))))
    lines.append(R(merge(bMid(3, 50, 'Execution policy blocks script'), bMid(53, 99, 'Get-ExecutionPolicy -List'))))
    lines.append(R(merge(bMid(3, 50, 'Module not found on remote host'), bMid(53, 99, '$Error[0] | Format-List *'))))
    lines.append(R(merge(bMid(3, 50, 'WinRM: Access is denied'), bMid(53, 99, 'Test-WSMan <host> -UseSSL'))))
    lines.append(R(merge(bMid(3, 50, 'Credential prompt in non-interactive'), bMid(53, 99, 'Get-PSSessionConfiguration'))))
    lines.append(R(merge(bMid(3, 50, 'Double-hop auth failure'), bMid(53, 99, 'Invoke-Command -Verbose'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Double-hop = remoting from host A to B to C; Kerberos fails; use JEA or resource-based KCD')))
    lines.append(R(bMid(3, 99, 'KCD        = Kerberos Constrained Delegation; allows service on B to request tickets for C')))
    lines.append(R(bMid(3, 99, '$Error[0]  = most recent error in session; always check before escalating')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/troubleshooting/common-issues', 'docs/automation/powershell/troubleshooting/common-issues/index.md', 'PowerShell common issues')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Common Issues'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Most frequent PowerShell failures and their fixes')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Script cannot be loaded because running scripts is disabled')))
    lines.append(R(bMid(3, 99, 'Fix: Set-ExecutionPolicy RemoteSigned -Scope CurrentUser')))
    lines.append(R(bMid(3, 99, 'Note: use -Scope Process for temporary bypass without changing machine policy')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: WinRM Access is denied')))
    lines.append(R(bMid(3, 99, 'Cause A: user not in WinRM access DACL → fix: Set-PSSessionConfiguration permissions')))
    lines.append(R(bMid(3, 99, 'Cause B: account locked or password expired → fix: unlock AD account, reset password')))
    lines.append(R(bMid(3, 99, 'Cause C: HTTPS cert invalid → fix: renew cert, update WinRM listener')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Module not found after installation')))
    lines.append(R(bMid(3, 99, 'Cause A: installed under different user scope → fix: Install-Module -Scope AllUsers')))
    lines.append(R(bMid(3, 99, 'Cause B: PSModulePath does not include install directory → fix: add path to env var')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/troubleshooting/diagnostics', 'docs/automation/powershell/troubleshooting/diagnostics/index.md', 'PowerShell diagnostics')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Diagnostics'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PowerShell diagnostic sequence: check error object → enable verbose → trace script execution')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Error Inspection'), bMid(53, 99, 'Trace and Debug'))))
    lines.append(R(merge(bMid(3, 50, '$Error[0] | Format-List *'), bMid(53, 99, 'Set-PSDebug -Trace 2'))))
    lines.append(R(merge(bMid(3, 50, '$Error[0].InnerException'), bMid(53, 99, '$VerbosePreference = Continue'))))
    lines.append(R(merge(bMid(3, 50, '$Error[0].ScriptStackTrace'), bMid(53, 99, 'Set-StrictMode -Version Latest'))))
    lines.append(R(merge(bMid(3, 50, 'Resolve-Error function (ISE)'), bMid(53, 99, 'Start-Transcript for full log'))))
    lines.append(R(merge(bMid(3, 50, 'Get-PSCallStack (in debugger)'), bMid(53, 99, 'Test-Path, Test-NetConnection'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Set-PSDebug -Trace 2 = traces every line executed with variable assignments; very verbose')))
    lines.append(R(bMid(3, 99, 'Set-StrictMode       = raises errors on undefined vars and bad index; catches bugs early')))
    lines.append(R(bMid(3, 99, 'ScriptStackTrace     = call stack at the point of error; shows which function called what')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/powershell/troubleshooting/escalation', 'docs/automation/powershell/troubleshooting/escalation/index.md', 'PowerShell escalation')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'PowerShell — Escalation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Escalate PowerShell issues to: infra team (remoting), vendor support (module bugs), Microsoft')))
    lines.append(R(bMid(3, 99, 'PowerShell Core issues: GitHub.com/PowerShell/PowerShell — file issue with repro steps')))
    lines.append(R(bMid(3, 99, 'Module issues: vendor support portal (VMware, Dell, Microsoft) — include module version + error')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Escalation Triggers'), bMid(53, 99, 'Info to Gather'))))
    lines.append(R(merge(bMid(3, 50, 'PS crashes / hangs on host'), bMid(53, 99, '$PSVersionTable output'))))
    lines.append(R(merge(bMid(3, 50, 'Module API returns wrong data'), bMid(53, 99, 'Get-InstalledModule list'))))
    lines.append(R(merge(bMid(3, 50, 'WinRM broken after OS update'), bMid(53, 99, 'winrm get config output'))))
    lines.append(R(merge(bMid(3, 50, 'JEA endpoint stops responding'), bMid(53, 99, 'Get-PSSessionConfiguration'))))
    lines.append(R(merge(bMid(3, 50, 'DSC compilation errors'), bMid(53, 99, 'Get-DscConfigurationStatus'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'PS GitHub issues = file bugs at github.com/PowerShell/PowerShell with minimal repro script')))
    lines.append(R(bMid(3, 99, 'DSC status       = Get-DscConfigurationStatus; shows last enactment result and error')))
    lines.append(R(bMid(3, 99, 'WinRM event log  = Event Viewer → Microsoft → Windows → WinRM for connection error details')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/index', 'docs/automation/python/index.md', 'Python overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Infrastructure Scripting and Automation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python: dominant language for infrastructure automation, API integration, and data processing')))
    lines.append(R(bMid(3, 99, 'Virtual environments: isolate dependencies per project; venv (stdlib) or conda/poetry')))
    lines.append(R(bMid(3, 99, 'Key infra libraries: boto3 (AWS), paramiko (SSH), requests (HTTP), fabric (remote exec)')))
    lines.append(R(bMid(3, 99, 'Testing: pytest for unit and integration tests; mock with unittest.mock or pytest-mock')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Architecture'), bMid(36, 66, 'Operations'), bMid(69, 99, 'Security'))))
    lines.append(R(merge(bMid(3, 33, 'venv + pip + poetry'), bMid(36, 66, 'Script management'), bMid(69, 99, 'Secrets: not in code'))))
    lines.append(R(merge(bMid(3, 33, 'Library ecosystem'), bMid(36, 66, 'Package updates'), bMid(69, 99, 'Dependency audit'))))
    lines.append(R(merge(bMid(3, 33, 'Type hints + mypy'), bMid(36, 66, 'pytest test suite'), bMid(69, 99, 'bandit static scan'))))
    lines.append(R(merge(bMid(3, 33, 'Standard modules'), bMid(36, 66, 'CI integration'), bMid(69, 99, 'Input validation'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'venv          = built-in virtual environment; python3 -m venv .venv && source .venv/bin/activate')))
    lines.append(R(bMid(3, 99, 'pip           = package installer; pip install -r requirements.txt')))
    lines.append(R(bMid(3, 99, 'Poetry        = dependency management with lock file; pyproject.toml; poetry add/install')))
    lines.append(R(bMid(3, 99, 'boto3         = AWS SDK for Python; session, client, resource interfaces')))
    lines.append(R(bMid(3, 99, 'paramiko      = SSH2 protocol library; SSHClient, SFTPClient; used for remote command exec')))
    lines.append(R(bMid(3, 99, 'requests      = HTTP library; Session, get/post/put; urllib3 under the hood')))
    lines.append(R(bMid(3, 99, 'fabric        = SSH command execution framework; built on Paramiko; task-based')))
    lines.append(R(bMid(3, 99, 'pytest        = testing framework; fixtures, parametrize, plugins ecosystem')))
    lines.append(R(bMid(3, 99, 'mypy          = static type checker; reads type hints; catches type errors before runtime')))
    lines.append(R(bMid(3, 99, 'ruff          = fast Python linter and formatter; replaces flake8/black/isort')))
    lines.append(R(bMid(3, 99, 'bandit        = security-focused linter; detects common vulnerabilities in Python code')))
    lines.append(R(bMid(3, 99, 'pyproject.toml= modern project config file; defines metadata, deps, tool settings')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/architecture/index', 'docs/automation/python/architecture/index.md', 'Python architecture overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Architecture'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python architecture: interpreter + stdlib + virtual environment + installed packages')))
    lines.append(R(bMid(3, 99, 'Package management: pip installs from PyPI; poetry/pipenv add lock file for reproducibility')))
    lines.append(R(bMid(3, 99, 'Project layout: src/ layout preferred; separate tests/, docs/, scripts/ directories')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'How It Works'), bMid(53, 99, 'Design Standards'))))
    lines.append(R(merge(bMid(3, 50, 'Script → CPython interpreter'), bMid(53, 99, 'One venv per project'))))
    lines.append(R(merge(bMid(3, 50, 'Imports from sys.path'), bMid(53, 99, 'Type hints on all functions'))))
    lines.append(R(merge(bMid(3, 50, 'Package: __init__.py'), bMid(53, 99, 'Docstrings: Google style'))))
    lines.append(R(merge(bMid(3, 50, 'Async: asyncio event loop'), bMid(53, 99, 'pytest test coverage >80%'))))
    lines.append(R(merge(bMid(3, 50, 'GIL: true parallel via mp'), bMid(53, 99, 'ruff lint + mypy in CI'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical: CPython runs on any OS; no compiled artefacts needed for distribution via source')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/architecture/how-it-works', 'docs/automation/python/architecture/how-it-works/index.md', 'Python how it works')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — How It Works'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'CPython: source → bytecode (.pyc) → interpreted by CPython VM; no AOT compilation')))
    lines.append(R(bMid(3, 99, 'Import system: finds modules via sys.path; .pth files extend path; namespace packages')))
    lines.append(R(bMid(3, 99, 'async/await: coroutines on asyncio event loop; use for I/O-bound concurrency (HTTP, SSH)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Execution'), bMid(36, 66, 'Concurrency'), bMid(69, 99, 'Packaging'))))
    lines.append(R(merge(bMid(3, 33, 'Source → bytecode (.pyc)'), bMid(36, 66, 'threading (GIL bound)'), bMid(69, 99, 'pyproject.toml'))))
    lines.append(R(merge(bMid(3, 33, 'CPython VM interprets'), bMid(36, 66, 'multiprocessing (true //'), bMid(69, 99, 'pip install -e . (dev)'))))
    lines.append(R(merge(bMid(3, 33, 'sys.path import search'), bMid(36, 66, 'asyncio (I/O bound)'), bMid(69, 99, 'build + twine publish'))))
    lines.append(R(merge(bMid(3, 33, 'PYTHONDONTWRITEBYTECODE'), bMid(36, 66, 'concurrent.futures'), bMid(69, 99, 'wheel + sdist'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'GIL         = Global Interpreter Lock; prevents true thread parallelism in CPython; use multiprocessing')))
    lines.append(R(bMid(3, 99, 'asyncio     = stdlib event loop; use await with async-native libraries (aiohttp, aioboto3)')))
    lines.append(R(bMid(3, 99, 'concurrent.futures = ThreadPoolExecutor / ProcessPoolExecutor; simpler parallelism API')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/architecture/design-standards', 'docs/automation/python/architecture/design-standards/index.md', 'Python design standards')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Design Standards'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python design standards: PEP 8 style, type hints, docstrings, testing, and dependency pinning')))
    lines.append(R(bMid(3, 99, 'Use ruff for linting and formatting (replaces flake8 + black + isort); run in CI')))
    lines.append(R(bMid(3, 99, 'All secrets via environment variables or secret manager — never hardcoded in source')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Code Style'), bMid(53, 99, 'Project Standards'))))
    lines.append(R(merge(bMid(3, 50, 'PEP 8 / ruff enforced in CI'), bMid(53, 99, 'pyproject.toml at repo root'))))
    lines.append(R(merge(bMid(3, 50, 'Type hints on all public funcs'), bMid(53, 99, 'Lock file: poetry.lock or pip compile'))))
    lines.append(R(merge(bMid(3, 50, 'Google-style docstrings'), bMid(53, 99, 'pytest coverage >= 80%'))))
    lines.append(R(merge(bMid(3, 50, 'Max line length: 88 (black default)'), bMid(53, 99, 'bandit -r src/ in CI'))))
    lines.append(R(merge(bMid(3, 50, 'if __name__ == "__main__":'), bMid(53, 99, 'CHANGELOG.md for releases'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'ruff         = Rust-based linter; ruff check . --fix; ruff format .; replaces many tools')))
    lines.append(R(bMid(3, 99, 'mypy         = type checker; mypy src/ --strict; catches bugs without running code')))
    lines.append(R(bMid(3, 99, 'pip compile  = pip-tools; pip-compile requirements.in → requirements.txt with pinned deps')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/architecture/integrations', 'docs/automation/python/architecture/integrations/index.md', 'Python integrations')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Integrations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python integrates with cloud, storage, monitoring, and ITSM platforms via vendor SDKs')))
    lines.append(R(bMid(3, 99, 'AWS: boto3; VMware: pyVmomi; NetApp: netapp-ontap; Pure: purestorage SDK; Dell: requests+REST')))
    lines.append(R(bMid(3, 99, 'CI/CD: scripts run in GitHub Actions, GitLab CI, Jenkins; pass results via exit codes + JSON')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Cloud SDKs'), bMid(36, 66, 'Infra SDKs'), bMid(69, 99, 'Monitoring / ITSM'))))
    lines.append(R(merge(bMid(3, 33, 'boto3 (AWS)'), bMid(36, 66, 'pyVmomi (vSphere)'), bMid(69, 99, 'requests → Grafana API'))))
    lines.append(R(merge(bMid(3, 33, 'azure-sdk-for-python'), bMid(36, 66, 'netapp-ontap'), bMid(69, 99, 'PyServiceNow'))))
    lines.append(R(merge(bMid(3, 33, 'google-cloud-* libs'), bMid(36, 66, 'purestorage SDK'), bMid(69, 99, 'Jira python client'))))
    lines.append(R(merge(bMid(3, 33, 'paramiko (SSH)'), bMid(36, 66, 'netmiko (network)'), bMid(69, 99, 'Slack SDK (webhooks)'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pyVmomi     = VMware vSphere Python SDK; SmartConnect to vCenter; traverse managed objects')))
    lines.append(R(bMid(3, 99, 'netmiko     = multi-vendor network device SSH library; supports Cisco, Arista, Juniper, etc.')))
    lines.append(R(bMid(3, 99, 'Exit codes  = scripts should exit 0 on success, 1+ on failure; CI checks exit code')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/index', 'docs/automation/python/operations/index.md', 'Python operations overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Operations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python operations: venv management, package updates, test runs, script deployment')))
    lines.append(R(bMid(3, 99, 'Day-to-day: activate venv, install/update deps, run pytest, lint with ruff, type-check mypy')))
    lines.append(R(bMid(3, 99, 'Deployment: copy scripts to target or install as package; use entry_points for CLI tools')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Environment Operations'), bMid(53, 99, 'Package Operations'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -m venv .venv'), bMid(53, 99, 'pip install -r requirements.txt'))))
    lines.append(R(merge(bMid(3, 50, 'source .venv/bin/activate'), bMid(53, 99, 'pip list --outdated'))))
    lines.append(R(merge(bMid(3, 50, '.venv/Scripts/activate (Win)'), bMid(53, 99, 'pip install -U <package>'))))
    lines.append(R(merge(bMid(3, 50, 'deactivate (exit venv)'), bMid(53, 99, 'pip-compile (pip-tools)'))))
    lines.append(R(merge(bMid(3, 50, 'python -m pytest tests/'), bMid(53, 99, 'safety check (CVE scan)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pip-tools    = pip-compile pins transitive deps; pip-sync installs exactly the lock file')))
    lines.append(R(bMid(3, 99, 'safety       = checks installed packages against known CVE database')))
    lines.append(R(bMid(3, 99, 'entry_points = pyproject.toml: [project.scripts] my-tool = "mypackage.cli:main"')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/backup-restore', 'docs/automation/python/operations/backup-restore/index.md', 'Python backup and restore')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Backup & Restore'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python script backup: git is the source of truth; include lock file for reproducibility')))
    lines.append(R(bMid(3, 99, 'requirements.txt or poetry.lock pins exact dep versions; re-create venv from lock on restore')))
    lines.append(R(bMid(3, 99, 'Restore: git clone → python3 -m venv .venv → pip install -r requirements.txt → verify')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'What to Back Up'), bMid(53, 99, 'Restore Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Git repo (all .py + config)'), bMid(53, 99, '1. git clone <repo>'))))
    lines.append(R(merge(bMid(3, 50, 'requirements.txt or poetry.lock'), bMid(53, 99, '2. python3 -m venv .venv'))))
    lines.append(R(merge(bMid(3, 50, 'pyproject.toml configuration'), bMid(53, 99, '3. pip install -r requirements.txt'))))
    lines.append(R(merge(bMid(3, 50, 'Environment variable names'), bMid(53, 99, '4. Re-inject secrets (env vars)'))))
    lines.append(R(merge(bMid(3, 50, 'Secrets: external vault (not git)'), bMid(53, 99, '5. pytest to verify'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Never commit   = .venv/, __pycache__/, *.pyc, .env files — add to .gitignore')))
    lines.append(R(bMid(3, 99, 'Lock file      = commit requirements.txt (pip-compile) or poetry.lock to git always')))
    lines.append(R(bMid(3, 99, 'Python version = document in .python-version (pyenv) or pyproject.toml requires-python')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/cli-reference', 'docs/automation/python/operations/cli-reference/index.md', 'Python CLI reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — CLI Reference'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Essential Python CLI commands for development, testing, and package management')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Python and venv'), bMid(53, 99, 'pip'))))
    lines.append(R(merge(bMid(3, 50, 'python3 --version'), bMid(53, 99, 'pip install <package>'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -m venv .venv'), bMid(53, 99, 'pip install -r requirements.txt'))))
    lines.append(R(merge(bMid(3, 50, 'python3 script.py'), bMid(53, 99, 'pip list --outdated'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -c "import boto3"'), bMid(53, 99, 'pip show <package>'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -m pdb script.py'), bMid(53, 99, 'pip freeze > requirements.txt'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'pytest'), bMid(53, 99, 'Code Quality'))))
    lines.append(R(merge(bMid(3, 50, 'pytest tests/'), bMid(53, 99, 'ruff check . --fix'))))
    lines.append(R(merge(bMid(3, 50, 'pytest -v -k test_name'), bMid(53, 99, 'ruff format .'))))
    lines.append(R(merge(bMid(3, 50, 'pytest --cov=src tests/'), bMid(53, 99, 'mypy src/ --strict'))))
    lines.append(R(merge(bMid(3, 50, 'pytest -x (stop on first fail)'), bMid(53, 99, 'bandit -r src/'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pdb          = Python Debugger; breakpoint() in code or python3 -m pdb script.py')))
    lines.append(R(bMid(3, 99, '--cov        = pytest-cov plugin; generates coverage report; set threshold in pyproject.toml')))
    lines.append(R(bMid(3, 99, 'ruff         = replaces flake8 + black + isort; configured in [tool.ruff] in pyproject.toml')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/health-checks', 'docs/automation/python/operations/health-checks/index.md', 'Python health checks')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Health Checks'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python health checks: verify interpreter version, venv, dependency currency, test pass rate')))
    lines.append(R(bMid(3, 99, 'CI pipeline is the primary health gate: ruff + mypy + bandit + pytest must all pass')))
    lines.append(R(bMid(3, 99, 'Dependency audit: pip list --outdated; safety check; monthly CVE scan')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Environment Checks'), bMid(53, 99, 'Quality Checks'))))
    lines.append(R(merge(bMid(3, 50, 'python3 --version (>=3.11)'), bMid(53, 99, 'pytest (all tests pass)'))))
    lines.append(R(merge(bMid(3, 50, 'pip list --outdated'), bMid(53, 99, 'ruff check . (zero errors)'))))
    lines.append(R(merge(bMid(3, 50, 'safety check (CVE scan)'), bMid(53, 99, 'mypy src/ (zero errors)'))))
    lines.append(R(merge(bMid(3, 50, 'python -c "import <lib>"'), bMid(53, 99, 'bandit -r src/ (zero high)'))))
    lines.append(R(merge(bMid(3, 50, 'Check .python-version match'), bMid(53, 99, 'Coverage report >= 80%'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'safety check   = queries PyPI advisory database; reports known CVEs in installed packages')))
    lines.append(R(bMid(3, 99, '.python-version= pyenv file; records required Python version; auto-activates with pyenv')))
    lines.append(R(bMid(3, 99, 'Dependabot     = GitHub service; auto-creates PRs to update dependencies weekly')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/install-upgrade', 'docs/automation/python/operations/install-upgrade/index.md', 'Python install and upgrade')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Install & Upgrade'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Install Python: pyenv for version management; system Python for server automation')))
    lines.append(R(bMid(3, 99, 'Upgrade Python version: pyenv install 3.12.3; pyenv local 3.12.3; re-create venv')))
    lines.append(R(bMid(3, 99, 'Upgrade packages: pip install -U <pkg>; or poetry update; test after every upgrade')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Python Version Install'), bMid(53, 99, 'Package Upgrade'))))
    lines.append(R(merge(bMid(3, 50, 'pyenv install 3.12.3'), bMid(53, 99, 'pip install -U <package>'))))
    lines.append(R(merge(bMid(3, 50, 'pyenv global 3.12.3'), bMid(53, 99, 'pip install -r requirements.txt'))))
    lines.append(R(merge(bMid(3, 50, 'pyenv local 3.12.3 (per project)'), bMid(53, 99, 'poetry update <package>'))))
    lines.append(R(merge(bMid(3, 50, 'rm -rf .venv && re-create'), bMid(53, 99, 'pip-compile --upgrade'))))
    lines.append(R(merge(bMid(3, 50, 'pip install -r requirements.txt'), bMid(53, 99, 'Run pytest after upgrade'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pyenv        = Python version manager; install multiple versions side-by-side; per-dir version')))
    lines.append(R(bMid(3, 99, 'EOL tracking = python.org/downloads; upgrade before EOL date; PS 3.8 EOL Oct 2024')))
    lines.append(R(bMid(3, 99, 'Re-create venv= after Python upgrade; venv is tied to a specific interpreter binary')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/procedures', 'docs/automation/python/operations/procedures/index.md', 'Python procedures')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Procedures'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Common Python operational procedures: new project setup, dependency audit, release packaging')))
    lines.append(R(bMid(3, 99, 'New project: git init → pyproject.toml → venv → install deps → first test → CI pipeline')))
    lines.append(R(bMid(3, 99, 'Release: bump version → update CHANGELOG → tag → build wheel → publish to internal PyPI')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'New Project Setup'), bMid(53, 99, 'Release Procedure'))))
    lines.append(R(merge(bMid(3, 50, '1. git init + .gitignore'), bMid(53, 99, '1. Bump version in pyproject.toml'))))
    lines.append(R(merge(bMid(3, 50, '2. pyproject.toml (poetry init)'), bMid(53, 99, '2. Update CHANGELOG.md'))))
    lines.append(R(merge(bMid(3, 50, '3. python3 -m venv .venv'), bMid(53, 99, '3. git tag v1.2.3 + push'))))
    lines.append(R(merge(bMid(3, 50, '4. poetry install (dev deps)'), bMid(53, 99, '4. python -m build'))))
    lines.append(R(merge(bMid(3, 50, '5. Add CI workflow (.yml)'), bMid(53, 99, '5. twine upload dist/*'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'twine        = package upload tool; uploads wheel/sdist to PyPI or internal Artifactory')))
    lines.append(R(bMid(3, 99, 'build        = python -m build; creates dist/*.whl and dist/*.tar.gz')))
    lines.append(R(bMid(3, 99, 'internal PyPI= host with Nexus, Artifactory, or devpi; configure with pip.conf index-url')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/operations/scripts', 'docs/automation/python/operations/scripts/index.md', 'Python scripts reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Scripts'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python utility scripts for infrastructure automation: AWS reporting, storage health, AD queries')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'AWS Scripts'), bMid(36, 66, 'Storage Scripts'), bMid(69, 99, 'Network Scripts'))))
    lines.append(R(merge(bMid(3, 33, 'ec2_inventory.py'), bMid(36, 66, 'unity_health.py'), bMid(69, 99, 'bgp_prefix_check.py'))))
    lines.append(R(merge(bMid(3, 33, 'sg_audit.py'), bMid(36, 66, 'netapp_capacity.py'), bMid(69, 99, 'interface_errors.py'))))
    lines.append(R(merge(bMid(3, 33, 'cost_report.py'), bMid(36, 66, 'snapshot_cleanup.py'), bMid(69, 99, 'dns_check.py'))))
    lines.append(R(merge(bMid(3, 33, 's3_lifecycle.py'), bMid(36, 66, 'replication_lag.py'), bMid(69, 99, 'cert_expiry.py'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Script structure = argparse for CLI args; logging module for output; sys.exit() return codes')))
    lines.append(R(bMid(3, 99, 'boto3 pattern    = boto3.Session(profile_name=args.profile).client("ec2")')))
    lines.append(R(bMid(3, 99, 'Output format    = JSON for machine parsing; tabulate/rich for human-readable terminal output')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/security/index', 'docs/automation/python/security/index.md', 'Python security overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Security'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python security: no hardcoded secrets, dependency CVE scanning, input validation, bandit scan')))
    lines.append(R(bMid(3, 99, 'Secrets: os.environ["KEY"] or SecretManager SDK — never in source or config files in repo')))
    lines.append(R(bMid(3, 99, 'Supply chain: pin all dependencies; scan with safety or pip-audit; use trusted index only')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Secrets'), bMid(36, 66, 'Code Security'), bMid(69, 99, 'Supply Chain'))))
    lines.append(R(merge(bMid(3, 33, 'os.environ for secrets'), bMid(36, 66, 'bandit -r src/'), bMid(69, 99, 'Pin all deps'))))
    lines.append(R(merge(bMid(3, 33, 'AWS SecretsManager SDK'), bMid(36, 66, 'No eval() / exec()'), bMid(69, 99, 'pip-audit / safety'))))
    lines.append(R(merge(bMid(3, 33, 'python-dotenv (.env local)'), bMid(36, 66, 'Validate all input'), bMid(69, 99, 'Only PyPI / internal'))))
    lines.append(R(merge(bMid(3, 33, 'No secrets in git history'), bMid(36, 66, 'subprocess: no shell=True'), bMid(69, 99, 'Dependabot PRs'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'bandit       = security linter; detects hardcoded secrets, SQL injection risks, unsafe calls')))
    lines.append(R(bMid(3, 99, 'pip-audit    = checks installed packages against OSV vulnerability database')))
    lines.append(R(bMid(3, 99, 'shell=True   = subprocess with shell=True risks command injection; always use list args instead')))
    lines.append(R(bMid(3, 99, 'pre-commit   = hooks for bandit/ruff/gitleaks run before each git commit')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/security/access-control', 'docs/automation/python/security/access-control/index.md', 'Python access control')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Access Control'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python access control: who can run scripts, API auth, file permissions, repo branch protection')))
    lines.append(R(bMid(3, 99, 'Scripts with destructive capability: require explicit --confirm flag; prompt for prod environment')))
    lines.append(R(bMid(3, 99, 'API auth: use IAM roles (AWS), service principals (Azure), service accounts (GCP/k8s)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Script Access Controls'), bMid(53, 99, 'API Auth Patterns'))))
    lines.append(R(merge(bMid(3, 50, '--dry-run flag for preview'), bMid(53, 99, 'boto3: IAM role via EC2 instance'))))
    lines.append(R(merge(bMid(3, 50, '--confirm for destructive ops'), bMid(53, 99, 'requests: Bearer token header'))))
    lines.append(R(merge(bMid(3, 50, 'Env var for target env check'), bMid(53, 99, 'paramiko: SSH key auth only'))))
    lines.append(R(merge(bMid(3, 50, 'Restrict executable: chmod 750'), bMid(53, 99, 'No username+password in code'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, '--dry-run    = show what would change without making changes; implement in all scripts')))
    lines.append(R(bMid(3, 99, 'IAM role     = EC2/Lambda instance profile; boto3 picks up credentials automatically')))
    lines.append(R(bMid(3, 99, 'chmod 750    = owner execute, group execute, no world access; protect sensitive scripts')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/security/authentication', 'docs/automation/python/security/authentication/index.md', 'Python authentication')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Authentication'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python authentication: boto3 credential chain, paramiko SSH key, requests auth classes')))
    lines.append(R(bMid(3, 99, 'boto3 credential chain: env var → ~/.aws/credentials → IAM role → container credentials')))
    lines.append(R(bMid(3, 99, 'Never use access keys in code; prefer IAM roles (EC2) or OIDC (GitHub Actions)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'AWS Authentication'), bMid(53, 99, 'SSH and REST Auth'))))
    lines.append(R(merge(bMid(3, 50, 'IAM role: boto3.Session()'), bMid(53, 99, 'paramiko.RSAKey.from_private_key'))))
    lines.append(R(merge(bMid(3, 50, 'Assume role: STS client'), bMid(53, 99, 'requests.auth.HTTPBasicAuth'))))
    lines.append(R(merge(bMid(3, 50, 'boto3.Session(profile_name=X)'), bMid(53, 99, 'Bearer: headers={"Auth": "..."}'))))
    lines.append(R(merge(bMid(3, 50, 'No hardcoded access keys'), bMid(53, 99, 'OAuth2: requests-oauthlib'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'STS AssumeRole = boto3 STS client; returns temp credentials for cross-account access')))
    lines.append(R(bMid(3, 99, 'Credential chain= boto3 tries in order: env vars, shared file, container, EC2 metadata')))
    lines.append(R(bMid(3, 99, 'keyring library = OS keychain integration; python-keyring; store/retrieve API tokens safely')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/security/encryption', 'docs/automation/python/security/encryption/index.md', 'Python encryption')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Encryption'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python encryption: cryptography library for AES/RSA; TLS via ssl/requests; key management')))
    lines.append(R(bMid(3, 99, 'Use: cryptography library (PyCA); avoid: pycrypto (unmaintained), roll-your-own crypto')))
    lines.append(R(bMid(3, 99, 'Transport: requests library uses SSL by default; verify=True (default) validates certs')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Encryption Patterns'), bMid(53, 99, 'Key Management'))))
    lines.append(R(merge(bMid(3, 50, 'Fernet: symmetric AES-128-CBC'), bMid(53, 99, 'AWS KMS: boto3 encrypt/decrypt'))))
    lines.append(R(merge(bMid(3, 50, 'RSA: asymmetric (sign/verify)'), bMid(53, 99, 'Azure Key Vault: SDK'))))
    lines.append(R(merge(bMid(3, 50, 'PBKDF2: password hashing'), bMid(53, 99, 'HashiCorp Vault: hvac library'))))
    lines.append(R(merge(bMid(3, 50, 'hashlib: sha256/sha512 digests'), bMid(53, 99, 'Never store key in code or git'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Fernet       = symmetric auth encryption; from cryptography.fernet import Fernet')))
    lines.append(R(bMid(3, 99, 'hvac         = HashiCorp Vault Python client; read secrets, write, authenticate')))
    lines.append(R(bMid(3, 99, 'verify=False = requests flag to skip TLS verification; NEVER in production')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/security/hardening', 'docs/automation/python/security/hardening/index.md', 'Python hardening')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Hardening'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Harden Python automation: input validation, subprocess safety, dep pinning, pre-commit hooks')))
    lines.append(R(bMid(3, 99, 'subprocess: always use list args; never shell=True with user input; capture stderr')))
    lines.append(R(bMid(3, 99, 'YAML: use yaml.safe_load() not yaml.load(); prevents arbitrary Python object execution')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Code Hardening'), bMid(53, 99, 'Pipeline Hardening'))))
    lines.append(R(merge(bMid(3, 50, 'subprocess list args (no shell)'), bMid(53, 99, 'bandit + ruff in CI (block merge)'))))
    lines.append(R(merge(bMid(3, 50, 'yaml.safe_load() not yaml.load()'), bMid(53, 99, 'pre-commit: bandit, gitleaks'))))
    lines.append(R(merge(bMid(3, 50, 'Validate + type-cast all input'), bMid(53, 99, 'Dependabot or Renovate weekly'))))
    lines.append(R(merge(bMid(3, 50, 'Use pathlib, not os.path.join'), bMid(53, 99, 'SBOM: pip-licenses --format=json'))))
    lines.append(R(merge(bMid(3, 50, 'No pickle for untrusted data'), bMid(53, 99, 'pip-audit in CI for CVEs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pickle        = Python serialisation; NEVER deserialise untrusted pickle data (code execution risk)')))
    lines.append(R(bMid(3, 99, 'gitleaks      = pre-commit hook scanning for secrets in staged code before commit')))
    lines.append(R(bMid(3, 99, 'SBOM          = Software Bill of Materials; list all deps + versions for compliance audit')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/troubleshooting/index', 'docs/automation/python/troubleshooting/index.md', 'Python troubleshooting overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Troubleshooting'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python troubleshooting: import errors, type errors, dependency conflicts, API auth failures')))
    lines.append(R(bMid(3, 99, 'First step: check python3 --version and which python3 to confirm correct interpreter')))
    lines.append(R(bMid(3, 99, 'Enable verbose traceback: PYTHONTRACEMALLOC=1 or python3 -X tracemalloc script.py')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Common Issues'), bMid(53, 99, 'Diagnostic Commands'))))
    lines.append(R(merge(bMid(3, 50, 'ModuleNotFoundError'), bMid(53, 99, 'pip show <module>'))))
    lines.append(R(merge(bMid(3, 50, 'ImportError: version conflict'), bMid(53, 99, 'pip check (dep conflicts)'))))
    lines.append(R(merge(bMid(3, 50, 'AttributeError on AWS response'), bMid(53, 99, 'python3 -m pdb script.py'))))
    lines.append(R(merge(bMid(3, 50, 'botocore.exceptions.ClientError'), bMid(53, 99, 'print(json.dumps(resp, indent=2))'))))
    lines.append(R(merge(bMid(3, 50, 'SSL: CERTIFICATE_VERIFY_FAILED'), bMid(53, 99, 'python -c "import ssl; ssl.SSLContext"'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'pip check       = verifies installed package compatibility; reports broken requirements')))
    lines.append(R(bMid(3, 99, 'which python3   = shows which interpreter is active; must point to venv bin/ in active venv')))
    lines.append(R(bMid(3, 99, 'ClientError     = boto3 API error; check error["Error"]["Code"] for specific AWS error code')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/troubleshooting/common-issues', 'docs/automation/python/troubleshooting/common-issues/index.md', 'Python common issues')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Common Issues'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Most frequent Python automation failures and their fixes')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: ModuleNotFoundError: No module named <x>')))
    lines.append(R(bMid(3, 99, 'Cause A: venv not activated → fix: source .venv/bin/activate')))
    lines.append(R(bMid(3, 99, 'Cause B: package not installed → fix: pip install <package>')))
    lines.append(R(bMid(3, 99, 'Cause C: wrong interpreter → fix: which python3 (must show .venv path)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: boto3 NoCredentialsError')))
    lines.append(R(bMid(3, 99, 'Cause A: AWS env vars not set → fix: export AWS_ACCESS_KEY_ID and SECRET')))
    lines.append(R(bMid(3, 99, 'Cause B: profile name wrong → fix: boto3.Session(profile_name="correct-profile")')))
    lines.append(R(bMid(3, 99, 'Cause C: IAM role not attached → fix: check EC2 instance profile in AWS console')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: SSL: CERTIFICATE_VERIFY_FAILED')))
    lines.append(R(bMid(3, 99, 'Fix (corp proxy): requests.get(url, verify="/path/to/corp-ca-bundle.pem")')))
    lines.append(R(bMid(3, 99, 'Fix: export REQUESTS_CA_BUNDLE=/etc/ssl/certs/corp-ca.pem in shell profile')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/troubleshooting/diagnostics', 'docs/automation/python/troubleshooting/diagnostics/index.md', 'Python diagnostics')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Diagnostics'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Python diagnostic sequence: reproduce → inspect traceback → pdb debug → add logging')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Error Analysis'), bMid(53, 99, 'Debug Tools'))))
    lines.append(R(merge(bMid(3, 50, 'Read full traceback (last line first)'), bMid(53, 99, 'python3 -m pdb script.py'))))
    lines.append(R(merge(bMid(3, 50, 'traceback.print_exc() in except'), bMid(53, 99, 'breakpoint() in code'))))
    lines.append(R(merge(bMid(3, 50, 'logging.exception("msg") logs tb'), bMid(53, 99, 'ipdb (IPython debugger)'))))
    lines.append(R(merge(bMid(3, 50, 'pip check (dep conflicts)'), bMid(53, 99, 'py-spy top (CPU profiler)'))))
    lines.append(R(merge(bMid(3, 50, 'python3 -W error to catch warns'), bMid(53, 99, 'memory_profiler (memory)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'breakpoint()   = built-in (3.7+); drops into pdb at that line; n=next, c=continue, p var')))
    lines.append(R(bMid(3, 99, 'py-spy         = sampling profiler; py-spy top --pid <pid>; no code changes needed')))
    lines.append(R(bMid(3, 99, '-W error       = treat warnings as errors; catches deprecation warnings before they break')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/python/troubleshooting/escalation', 'docs/automation/python/troubleshooting/escalation/index.md', 'Python escalation')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Python — Escalation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Escalate Python issues: CPython bugs (bugs.python.org), library bugs (GitHub issues)')))
    lines.append(R(bMid(3, 99, 'boto3/botocore issues: github.com/boto/boto3; include AWS region, service, error code')))
    lines.append(R(bMid(3, 99, 'Internal escalation: infra team for network/SSL issues; security team for secret exposure')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Escalation Triggers'), bMid(53, 99, 'Info to Gather'))))
    lines.append(R(merge(bMid(3, 50, 'Dependency CVE found in prod'), bMid(53, 99, 'pip list output'))))
    lines.append(R(merge(bMid(3, 50, 'CPython segfault or crash'), bMid(53, 99, 'python3 --version'))))
    lines.append(R(merge(bMid(3, 50, 'API returning unexpected data'), bMid(53, 99, 'Full traceback + request log'))))
    lines.append(R(merge(bMid(3, 50, 'Secret potentially exposed in log'), bMid(53, 99, 'Affected secret names + scope'))))
    lines.append(R(merge(bMid(3, 50, 'SSL broken after OS update'), bMid(53, 99, 'openssl version output'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'CVE response  = rotate affected secret immediately; audit access logs for the exposure window')))
    lines.append(R(bMid(3, 99, 'CPython bugs  = bugs.python.org; include minimal reproducible script and Python version')))
    lines.append(R(bMid(3, 99, 'boto3 issues  = github.com/boto/boto3/issues; label: needs-triage; include service + region')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/index', 'docs/automation/terraform/index.md', 'Terraform overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Infrastructure as Code'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform: HashiCorp IaC tool; declares resources in HCL; plan → apply workflow')))
    lines.append(R(bMid(3, 99, 'Provider plugins: AWS, Azure, GCP, VMware, NetApp, Kubernetes — one binary per provider')))
    lines.append(R(bMid(3, 99, 'State: JSON file tracking actual resource state; stored in S3 + DynamoDB or Terraform Cloud')))
    lines.append(R(bMid(3, 99, 'Modules: reusable resource groups; internal registry or Terraform Registry (public)')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'Architecture'), bMid(36, 66, 'Operations'), bMid(69, 99, 'Security'))))
    lines.append(R(merge(bMid(3, 33, 'HCL + providers'), bMid(36, 66, 'Plan / apply / destroy'), bMid(69, 99, 'State encryption'))))
    lines.append(R(merge(bMid(3, 33, 'State backend'), bMid(36, 66, 'Workspace management'), bMid(69, 99, 'Remote backend'))))
    lines.append(R(merge(bMid(3, 33, 'Module design'), bMid(36, 66, 'Import existing infra'), bMid(69, 99, 'Sentinel policies'))))
    lines.append(R(merge(bMid(3, 33, 'Dependency graph'), bMid(36, 66, 'State operations'), bMid(69, 99, 'Least-privilege auth'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'HCL         = HashiCorp Configuration Language; declarative, JSON-compatible; .tf files')))
    lines.append(R(bMid(3, 99, 'Provider    = plugin implementing CRUD operations for a platform API (AWS, vSphere, etc.)')))
    lines.append(R(bMid(3, 99, 'State file  = terraform.tfstate; maps resource configs to real-world resource IDs')))
    lines.append(R(bMid(3, 99, 'Plan        = terraform plan; shows what will be created/modified/destroyed; no changes made')))
    lines.append(R(bMid(3, 99, 'Apply       = terraform apply; executes the plan after confirmation or -auto-approve')))
    lines.append(R(bMid(3, 99, 'Module      = encapsulated resource group with inputs/outputs; reusable across configs')))
    lines.append(R(bMid(3, 99, 'Workspace   = isolated state environment; prod/staging/dev within same config')))
    lines.append(R(bMid(3, 99, 'Backend     = remote state storage; S3+DynamoDB (locking), GCS, Terraform Cloud, Azure Blob')))
    lines.append(R(bMid(3, 99, 'Sentinel    = policy-as-code; Terraform Cloud/Enterprise; enforce tagging, region, cost limits')))
    lines.append(R(bMid(3, 99, 'Data source = reads existing infrastructure without managing it; e.g. aws_ami, aws_vpc')))
    lines.append(R(bMid(3, 99, 'Output      = exported values from a module or root config; passed to other configs')))
    lines.append(R(bMid(3, 99, 'Variable    = parameterised input; .tfvars file, env var TF_VAR_name, or -var flag')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/architecture/index', 'docs/automation/terraform/architecture/index.md', 'Terraform architecture overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Architecture'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform architecture: CLI + core + provider plugins + remote state backend + target API')))
    lines.append(R(bMid(3, 99, 'Execution: CLI parses HCL → builds dependency graph → calls provider APIs in parallel')))
    lines.append(R(bMid(3, 99, 'Remote state: S3 bucket (versioned) + DynamoDB table (state lock) is the standard pattern')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'How It Works'), bMid(53, 99, 'Design Standards'))))
    lines.append(R(merge(bMid(3, 50, 'HCL parsed → resource graph'), bMid(53, 99, 'One module per resource type'))))
    lines.append(R(merge(bMid(3, 50, 'Provider downloaded (.terraform/)'), bMid(53, 99, 'Remote state always (no local)'))))
    lines.append(R(merge(bMid(3, 50, 'State read + plan computed'), bMid(53, 99, 'Pin provider versions'))))
    lines.append(R(merge(bMid(3, 50, 'Provider API calls in parallel'), bMid(53, 99, 'Separate state per workspace'))))
    lines.append(R(merge(bMid(3, 50, 'State updated after apply'), bMid(53, 99, 'Approved plan before apply (CI)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Physical: Terraform runs on CI runner or workstation; provider communicates with target APIs')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/architecture/how-it-works', 'docs/automation/terraform/architecture/how-it-works/index.md', 'Terraform how it works')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — How It Works'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'terraform init → plan → apply is the core workflow; destroy reverses apply')))
    lines.append(R(bMid(3, 99, 'init: downloads providers; plan: computes diff vs state; apply: calls provider CRUD APIs')))
    lines.append(R(bMid(3, 99, 'State locking: DynamoDB or backend-specific lock prevents concurrent applies')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'terraform init'), bMid(36, 66, 'terraform plan'), bMid(69, 99, 'terraform apply'))))
    lines.append(R(merge(bMid(3, 33, 'Download providers'), bMid(36, 66, 'Read current state'), bMid(69, 99, 'Confirm or -auto-approve'))))
    lines.append(R(merge(bMid(3, 33, 'Configure backend'), bMid(36, 66, 'Call provider read APIs'), bMid(69, 99, 'Provider CRUD calls'))))
    lines.append(R(merge(bMid(3, 33, 'Initialise modules'), bMid(36, 66, 'Compute resource diff'), bMid(69, 99, 'Update state file'))))
    lines.append(R(merge(bMid(3, 33, '.terraform.lock.hcl'), bMid(36, 66, 'Output plan file'), bMid(69, 99, 'Release state lock'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, '.terraform.lock.hcl = provider version lock file; commit to git for reproducible initialisation')))
    lines.append(R(bMid(3, 99, 'plan file           = terraform plan -out=tfplan; apply: terraform apply tfplan (no re-diff)')))
    lines.append(R(bMid(3, 99, 'State lock          = prevents concurrent terraform apply; acquired on apply, released on finish')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/architecture/design-standards', 'docs/automation/terraform/architecture/design-standards/index.md', 'Terraform design standards')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Design Standards'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform standards: consistent module structure, remote state, provider pinning, CI enforcement')))
    lines.append(R(bMid(3, 99, 'Directory layout: modules/ (reusable), environments/ (per-env root), shared/ (cross-env data)')))
    lines.append(R(bMid(3, 99, 'CI: terraform fmt -check → validate → tflint → checkov → plan → manual approval → apply')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'File Structure'), bMid(53, 99, 'Quality Rules'))))
    lines.append(R(merge(bMid(3, 50, 'main.tf: resources'), bMid(53, 99, 'terraform fmt -recursive'))))
    lines.append(R(merge(bMid(3, 50, 'variables.tf: all inputs'), bMid(53, 99, 'terraform validate'))))
    lines.append(R(merge(bMid(3, 50, 'outputs.tf: all exports'), bMid(53, 99, 'tflint: provider rules'))))
    lines.append(R(merge(bMid(3, 50, 'versions.tf: required_providers'), bMid(53, 99, 'checkov: security scan'))))
    lines.append(R(merge(bMid(3, 50, 'backend.tf: remote state config'), bMid(53, 99, 'No hardcoded credentials'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'tflint      = Terraform linter; checks for unused vars, deprecated syntax, provider rules')))
    lines.append(R(bMid(3, 99, 'checkov     = Bridgecrew IaC security scanner; checks for open SGs, unencrypted storage, etc.')))
    lines.append(R(bMid(3, 99, 'terraform-docs = auto-generate module README from variables.tf and outputs.tf')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/architecture/integrations', 'docs/automation/terraform/architecture/integrations/index.md', 'Terraform integrations')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Integrations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform integrates with CI/CD, secret managers, monitoring, and ITSM via providers and APIs')))
    lines.append(R(bMid(3, 99, 'GitHub Actions: terraform plan in PR check; terraform apply on merge to main')))
    lines.append(R(bMid(3, 99, 'Secrets: provider auth via env vars or OIDC; Vault provider for secret injection into resources')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'CI/CD'), bMid(36, 66, 'Secrets'), bMid(69, 99, 'Platforms'))))
    lines.append(R(merge(bMid(3, 33, 'GitHub Actions (OIDC)'), bMid(36, 66, 'Vault provider'), bMid(69, 99, 'AWS, Azure, GCP'))))
    lines.append(R(merge(bMid(3, 33, 'GitLab CI: plan/apply'), bMid(36, 66, 'env var: TF_VAR_x'), bMid(69, 99, 'VMware vSphere'))))
    lines.append(R(merge(bMid(3, 33, 'Atlantis (PR automation)'), bMid(36, 66, 'AWS SSM Parameter Store'), bMid(69, 99, 'NetApp, Pure, Dell'))))
    lines.append(R(merge(bMid(3, 33, 'Terraform Cloud/Enterprise'), bMid(36, 66, 'No secrets in .tfvars in git'), bMid(69, 99, 'Kubernetes, Helm'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Atlantis    = Terraform PR automation; runs plan on PR, apply on merge; self-hosted')))
    lines.append(R(bMid(3, 99, 'TF Cloud    = HashiCorp SaaS; remote execution, Sentinel policies, team RBAC, audit log')))
    lines.append(R(bMid(3, 99, 'Vault provider= reads secrets from Vault at apply time; writes dynamic credentials to state')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/index', 'docs/automation/terraform/operations/index.md', 'Terraform operations overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Operations'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform operations: init, plan, apply, state management, workspace management, import')))
    lines.append(R(bMid(3, 99, 'Day-to-day: create branch → modify .tf → plan → review → apply → commit')))
    lines.append(R(bMid(3, 99, 'State ops: terraform state list/show/mv/rm — use with caution; always backup state first')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Core Operations'), bMid(53, 99, 'State Operations'))))
    lines.append(R(merge(bMid(3, 50, 'terraform init'), bMid(53, 99, 'terraform state list'))))
    lines.append(R(merge(bMid(3, 50, 'terraform plan -out=tfplan'), bMid(53, 99, 'terraform state show <resource>'))))
    lines.append(R(merge(bMid(3, 50, 'terraform apply tfplan'), bMid(53, 99, 'terraform state mv A B'))))
    lines.append(R(merge(bMid(3, 50, 'terraform destroy -target=X'), bMid(53, 99, 'terraform state rm <resource>'))))
    lines.append(R(merge(bMid(3, 50, 'terraform workspace select prod'), bMid(53, 99, 'terraform import addr ID'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'terraform import = bring existing resource under Terraform management; writes state only')))
    lines.append(R(bMid(3, 99, 'terraform state rm= removes resource from state without destroying it; orphans the resource')))
    lines.append(R(bMid(3, 99, '-target          = apply/destroy specific resource; use sparingly; state can become inconsistent')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/backup-restore', 'docs/automation/terraform/operations/backup-restore/index.md', 'Terraform backup and restore')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Backup & Restore'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform backup: git repo for .tf code; S3 versioning for state file; .terraform.lock.hcl in git')))
    lines.append(R(bMid(3, 99, 'Enable S3 versioning on state bucket; DynamoDB table for lock; enable S3 access logging')))
    lines.append(R(bMid(3, 99, 'State restore: download previous version from S3, push with terraform state push')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'What to Back Up'), bMid(53, 99, 'Restore Steps'))))
    lines.append(R(merge(bMid(3, 50, 'Git repo (.tf + lock file)'), bMid(53, 99, '1. Identify state version in S3'))))
    lines.append(R(merge(bMid(3, 50, 'S3 bucket (versioned state)'), bMid(53, 99, '2. Download previous version'))))
    lines.append(R(merge(bMid(3, 50, 'DynamoDB table (lock metadata)'), bMid(53, 99, '3. terraform state push state.bak'))))
    lines.append(R(merge(bMid(3, 50, '.terraform.lock.hcl in git'), bMid(53, 99, '4. terraform plan (verify)'))))
    lines.append(R(merge(bMid(3, 50, 'tfvars (non-secret values)'), bMid(53, 99, '5. Correct any drift manually'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'S3 versioning   = preserves all state file versions; restore from Version ID in S3 console')))
    lines.append(R(bMid(3, 99, 'state push       = terraform state push <file>; overwrites remote state; use with caution')))
    lines.append(R(bMid(3, 99, 'State corruption = most common cause: concurrent apply or force-unlock; check state integrity first')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/cli-reference', 'docs/automation/terraform/operations/cli-reference/index.md', 'Terraform CLI reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — CLI Reference'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Essential Terraform CLI commands for daily infrastructure operations')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Workflow Commands'), bMid(53, 99, 'Utility Commands'))))
    lines.append(R(merge(bMid(3, 50, 'terraform init [-upgrade]'), bMid(53, 99, 'terraform fmt -recursive'))))
    lines.append(R(merge(bMid(3, 50, 'terraform plan -out=tfplan'), bMid(53, 99, 'terraform validate'))))
    lines.append(R(merge(bMid(3, 50, 'terraform apply tfplan'), bMid(53, 99, 'terraform graph | dot'))))
    lines.append(R(merge(bMid(3, 50, 'terraform destroy [-target=X]'), bMid(53, 99, 'terraform output -json'))))
    lines.append(R(merge(bMid(3, 50, 'terraform refresh'), bMid(53, 99, 'terraform providers lock'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'State Commands'), bMid(53, 99, 'Workspace Commands'))))
    lines.append(R(merge(bMid(3, 50, 'terraform state list'), bMid(53, 99, 'terraform workspace list'))))
    lines.append(R(merge(bMid(3, 50, 'terraform state show <res>'), bMid(53, 99, 'terraform workspace new prod'))))
    lines.append(R(merge(bMid(3, 50, 'terraform state mv A B'), bMid(53, 99, 'terraform workspace select prod'))))
    lines.append(R(merge(bMid(3, 50, 'terraform import addr id'), bMid(53, 99, 'terraform workspace show'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'terraform graph = exports dependency graph in DOT format; pipe to graphviz dot for visualisation')))
    lines.append(R(bMid(3, 99, 'terraform refresh= updates state to match real-world without applying config changes')))
    lines.append(R(bMid(3, 99, 'providers lock   = regenerates .terraform.lock.hcl with all platforms; use before committing')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/health-checks', 'docs/automation/terraform/operations/health-checks/index.md', 'Terraform health checks')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Health Checks'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform health checks: state drift, stale lock, provider version currency, CI pipeline pass')))
    lines.append(R(bMid(3, 99, 'Drift detection: terraform plan on schedule; alert if non-empty plan in stable environment')))
    lines.append(R(bMid(3, 99, 'Stale lock: terraform force-unlock <lock-id> after confirming no apply is running')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'State Health'), bMid(53, 99, 'Pipeline Health'))))
    lines.append(R(merge(bMid(3, 50, 'terraform plan (expect empty)'), bMid(53, 99, 'fmt -check: no format drift'))))
    lines.append(R(merge(bMid(3, 50, 'Check for stale lock in DynamoDB'), bMid(53, 99, 'validate: no config errors'))))
    lines.append(R(merge(bMid(3, 50, 'S3 versioning enabled'), bMid(53, 99, 'tflint: zero warnings'))))
    lines.append(R(merge(bMid(3, 50, 'terraform version (up to date)'), bMid(53, 99, 'checkov: zero failures'))))
    lines.append(R(merge(bMid(3, 50, 'Provider plugin version currency'), bMid(53, 99, 'Plan approved before apply'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Drift          = when actual infra state diverges from Terraform state; plan shows changes')))
    lines.append(R(bMid(3, 99, 'force-unlock   = removes DynamoDB lock entry; only run if certain no apply is in progress')))
    lines.append(R(bMid(3, 99, 'Scheduled plan = run terraform plan -detailed-exitcode in CI; exit 2 = changes detected')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/install-upgrade', 'docs/automation/terraform/operations/install-upgrade/index.md', 'Terraform install and upgrade')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Install & Upgrade'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Install Terraform: tfenv for version management; or direct binary from releases.hashicorp.com')))
    lines.append(R(bMid(3, 99, 'Upgrade: tfenv install 1.8.0; tfenv use 1.8.0; test with terraform init -upgrade')))
    lines.append(R(bMid(3, 99, 'Provider upgrade: update required_providers version constraint; terraform init -upgrade')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Terraform Install (tfenv)'), bMid(53, 99, 'Provider Upgrade'))))
    lines.append(R(merge(bMid(3, 50, 'brew install tfenv'), bMid(53, 99, 'Update versions.tf constraint'))))
    lines.append(R(merge(bMid(3, 50, 'tfenv install 1.8.0'), bMid(53, 99, 'terraform init -upgrade'))))
    lines.append(R(merge(bMid(3, 50, 'tfenv use 1.8.0'), bMid(53, 99, 'Review .terraform.lock.hcl diff'))))
    lines.append(R(merge(bMid(3, 50, 'echo "1.8.0" > .terraform-version'), bMid(53, 99, 'terraform plan (check no issues)'))))
    lines.append(R(merge(bMid(3, 50, 'terraform version (verify)'), bMid(53, 99, 'Commit updated lock file'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'tfenv           = Terraform version manager; .terraform-version file pins version per project')))
    lines.append(R(bMid(3, 99, '-upgrade        = init flag; re-downloads providers to latest matching version constraint')))
    lines.append(R(bMid(3, 99, 'required_version= constraint in versions.tf; prevents running wrong Terraform version')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/procedures', 'docs/automation/terraform/operations/procedures/index.md', 'Terraform procedures')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Procedures'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform procedures: new module creation, import existing infra, state migration, workspace setup')))
    lines.append(R(bMid(3, 99, 'New module: write main.tf, variables.tf, outputs.tf, versions.tf; add examples/; add tests/')))
    lines.append(R(bMid(3, 99, 'Import: terraform import resource.type.name <resource-id>; then terraform plan to verify')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Import Existing Resource'), bMid(53, 99, 'State Migration'))))
    lines.append(R(merge(bMid(3, 50, '1. Write resource block in .tf'), bMid(53, 99, '1. terraform state pull > old.tfstate'))))
    lines.append(R(merge(bMid(3, 50, '2. terraform import addr id'), bMid(53, 99, '2. Modify state JSON if needed'))))
    lines.append(R(merge(bMid(3, 50, '3. terraform plan (expect empty)'), bMid(53, 99, '3. terraform state push new.tfstate'))))
    lines.append(R(merge(bMid(3, 50, '4. Adjust config to match state'), bMid(53, 99, '4. terraform plan (verify)'))))
    lines.append(R(merge(bMid(3, 50, '5. Commit to git'), bMid(53, 99, '5. Run apply to reconcile'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'terraform import  = adds existing resource to state; does not generate .tf code (use config gen)')))
    lines.append(R(bMid(3, 99, 'Config generation = terraform plan -generate-config-out=generated.tf (TF 1.5+)')))
    lines.append(R(bMid(3, 99, 'state migration   = moving state between backends; terraform init -migrate-state')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/operations/scripts', 'docs/automation/terraform/operations/scripts/index.md', 'Terraform scripts reference')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Scripts'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform utility scripts: drift report, stale lock check, state backup, plan summary')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Operations Scripts'), bMid(53, 99, 'CI/CD Scripts'))))
    lines.append(R(merge(bMid(3, 50, 'drift_check.sh (plan + alert)'), bMid(53, 99, 'tf_plan_pr_comment.py'))))
    lines.append(R(merge(bMid(3, 50, 'backup_state.sh (S3 copy)'), bMid(53, 99, 'tf_apply_gate.sh'))))
    lines.append(R(merge(bMid(3, 50, 'unlock_stale.sh (force-unlock)'), bMid(53, 99, 'tf_fmt_check.sh'))))
    lines.append(R(merge(bMid(3, 50, 'list_drift.py (parse plan JSON)'), bMid(53, 99, 'checkov_report.sh'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Plan JSON     = terraform plan -out=tfplan; terraform show -json tfplan > plan.json')))
    lines.append(R(bMid(3, 99, 'PR comment    = use GitHub API or atlantis to post plan output as PR comment for review')))
    lines.append(R(bMid(3, 99, '-detailed-exitcode= exit 0: no changes, exit 1: error, exit 2: changes present')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/security/index', 'docs/automation/terraform/security/index.md', 'Terraform security overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Security'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform security: protect state, manage secrets, enforce least-privilege, scan IaC with checkov')))
    lines.append(R(bMid(3, 99, 'State file contains sensitive values; encrypt at rest (S3 SSE-KMS); restrict S3 bucket ACL')))
    lines.append(R(bMid(3, 99, 'Provider auth: OIDC (GitHub Actions → AWS) or IAM role; avoid long-lived access keys')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 33), bTop(36, 66), bTop(69, 99))))
    lines.append(R(merge(bMid(3, 33, 'State Security'), bMid(36, 66, 'Code Security'), bMid(69, 99, 'Auth Security'))))
    lines.append(R(merge(bMid(3, 33, 'S3 SSE-KMS encryption'), bMid(36, 66, 'checkov in CI'), bMid(69, 99, 'OIDC for CI auth'))))
    lines.append(R(merge(bMid(3, 33, 'S3 Block Public Access'), bMid(36, 66, 'tflint for lint'), bMid(69, 99, 'Least-privilege IAM'))))
    lines.append(R(merge(bMid(3, 33, 'S3 access logging'), bMid(36, 66, 'No secrets in .tfvars'), bMid(69, 99, 'No long-lived keys'))))
    lines.append(R(merge(bMid(3, 33, 'DynamoDB state lock'), bMid(36, 66, 'Sensitive = true vars'), bMid(69, 99, 'Sentinel policies (TFC)'))))
    lines.append(R(merge(bBot(3, 33), bBot(36, 66), bBot(69, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'sensitive = true = marks variable or output as sensitive; redacted in plan/apply output')))
    lines.append(R(bMid(3, 99, 'checkov       = scans Terraform configs for security issues: open SGs, no MFA, public S3')))
    lines.append(R(bMid(3, 99, 'Sentinel      = HashiCorp policy-as-code; enforces tagging, resource limits, approved regions')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/security/access-control', 'docs/automation/terraform/security/access-control/index.md', 'Terraform access control')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Access Control'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform access control: who can plan/apply, state file access, provider credential scope')))
    lines.append(R(bMid(3, 99, 'Separate IAM roles: read-only (plan) and read-write (apply); apply requires approval in CI')))
    lines.append(R(bMid(3, 99, 'S3 state bucket: restrict GetObject/PutObject to Terraform IAM role only; block public access')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'IAM Role Design'), bMid(53, 99, 'CI Access Control'))))
    lines.append(R(merge(bMid(3, 50, 'tf-plan-role: read-only to AWS'), bMid(53, 99, 'Plan: any branch, auto-run'))))
    lines.append(R(merge(bMid(3, 50, 'tf-apply-role: write permissions'), bMid(53, 99, 'Apply: main branch only'))))
    lines.append(R(merge(bMid(3, 50, 'S3: GetObject/PutObject/List'), bMid(53, 99, 'Required reviewers before apply'))))
    lines.append(R(merge(bMid(3, 50, 'DynamoDB: PutItem/DeleteItem'), bMid(53, 99, 'OIDC trust: repo+branch filter'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'OIDC trust  = AWS OIDC provider; trust policy sub: repo:org/repo:ref:refs/heads/main')))
    lines.append(R(bMid(3, 99, 'Plan role   = read-only; cannot modify state or infrastructure; safe for PR checks')))
    lines.append(R(bMid(3, 99, 'Apply role  = write; assumed only after PR approval and merge to main')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/security/authentication', 'docs/automation/terraform/security/authentication/index.md', 'Terraform authentication')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Authentication'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform provider authentication: OIDC (CI), IAM instance profile (EC2), CLI profile (local)')))
    lines.append(R(bMid(3, 99, 'AWS: aws provider picks up credential chain; for CI use OIDC role assumption')))
    lines.append(R(bMid(3, 99, 'vSphere: vsphere_user + vsphere_password via env vars; or vault-managed credentials')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Cloud Auth (OIDC)'), bMid(53, 99, 'Local Dev Auth'))))
    lines.append(R(merge(bMid(3, 50, 'GitHub OIDC → AWS AssumeRole'), bMid(53, 99, 'aws configure (named profile)'))))
    lines.append(R(merge(bMid(3, 50, 'azure/login OIDC → federated'), bMid(53, 99, 'export TF_VAR_vsphere_pw=...'))))
    lines.append(R(merge(bMid(3, 50, 'GCP: Workload Identity OIDC'), bMid(53, 99, 'export AWS_PROFILE=myprofile'))))
    lines.append(R(merge(bMid(3, 50, 'No stored access keys in CI'), bMid(53, 99, 'Vault: vault login then TF runs'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Credential chain= Terraform AWS provider uses same chain as boto3/aws CLI for auth')))
    lines.append(R(bMid(3, 99, 'TF_VAR_        = environment variable prefix; TF_VAR_vsphere_password maps to var.vsphere_password')))
    lines.append(R(bMid(3, 99, 'Vault agent    = injects secrets into env before terraform runs; no secrets in process args')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/security/encryption', 'docs/automation/terraform/security/encryption/index.md', 'Terraform encryption')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Encryption'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform encryption: state file encryption at rest (S3 SSE-KMS), transit TLS, sensitive outputs')))
    lines.append(R(bMid(3, 99, 'State encryption: server-side via S3 SSE-KMS; TF 1.7+ client-side: terraform encryption{}')))
    lines.append(R(bMid(3, 99, 'Sensitive outputs: never stored as plaintext in state; mark outputs sensitive = true')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'State Encryption'), bMid(53, 99, 'Sensitive Variables'))))
    lines.append(R(merge(bMid(3, 50, 'S3 SSE-KMS on state bucket'), bMid(53, 99, 'variable "x" { sensitive = true }'))))
    lines.append(R(merge(bMid(3, 50, 'Terraform 1.7+ encryption{}'), bMid(53, 99, 'output "x" { sensitive = true }'))))
    lines.append(R(merge(bMid(3, 50, 'AES-GCM key from KMS or Vault'), bMid(53, 99, 'Redacted in plan and apply log'))))
    lines.append(R(merge(bMid(3, 50, 'All TF API calls: TLS 1.2+'), bMid(53, 99, 'Still stored in state (encrypted)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'encryption block= TF 1.7+: encrypt state at Terraform level before S3; defence-in-depth')))
    lines.append(R(bMid(3, 99, 'SSE-KMS        = S3 server-side encryption with AWS KMS managed key; transparent to Terraform')))
    lines.append(R(bMid(3, 99, 'sensitive = true= prevents value from appearing in output; value still exists in state file')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/security/hardening', 'docs/automation/terraform/security/hardening/index.md', 'Terraform hardening')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Hardening'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform hardening: secure state backend, restrict apply access, scan configs, pin versions')))
    lines.append(R(bMid(3, 99, 'S3 bucket hardening: Block Public Access, versioning, SSE-KMS, access logging, MFA delete')))
    lines.append(R(bMid(3, 99, 'CI hardening: plan only on PR, apply only on main, required approval gate, audit log')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'State Backend Hardening'), bMid(53, 99, 'Config Hardening'))))
    lines.append(R(merge(bMid(3, 50, 'S3: Block Public Access on'), bMid(53, 99, 'Pin: required_version'))))
    lines.append(R(merge(bMid(3, 50, 'S3: SSE-KMS encryption'), bMid(53, 99, 'Pin: required_providers versions'))))
    lines.append(R(merge(bMid(3, 50, 'S3: versioning enabled'), bMid(53, 99, 'checkov: block PR on failure'))))
    lines.append(R(merge(bMid(3, 50, 'DynamoDB: IAM restrict'), bMid(53, 99, 'tfsec / tflint in CI'))))
    lines.append(R(merge(bMid(3, 50, 'CloudTrail on S3 state bucket'), bMid(53, 99, 'No local state file in git'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'MFA delete      = S3 bucket MFA Delete protection; prevents accidental state deletion')))
    lines.append(R(bMid(3, 99, 'tfsec           = open-source IaC security scanner; similar to checkov; check both')))
    lines.append(R(bMid(3, 99, 'CloudTrail on S3= logs every GetObject/PutObject on state bucket; full audit trail')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/troubleshooting/index', 'docs/automation/terraform/troubleshooting/index.md', 'Terraform troubleshooting overview')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Troubleshooting'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform troubleshooting: state errors, provider errors, plan/apply failures, lock errors')))
    lines.append(R(bMid(3, 99, 'Enable debug: TF_LOG=DEBUG terraform plan 2>&1 | tee tf-debug.log')))
    lines.append(R(bMid(3, 99, 'Provider errors: TF_LOG_PROVIDER=DEBUG for provider-level API call logging')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Common Issues'), bMid(53, 99, 'Diagnostic Commands'))))
    lines.append(R(merge(bMid(3, 50, 'Error acquiring state lock'), bMid(53, 99, 'terraform plan (verbose output)'))))
    lines.append(R(merge(bMid(3, 50, 'Provider authentication error'), bMid(53, 99, 'TF_LOG=DEBUG tf plan'))))
    lines.append(R(merge(bMid(3, 50, 'Resource already exists'), bMid(53, 99, 'terraform state show <resource>'))))
    lines.append(R(merge(bMid(3, 50, 'Cycle error in dependency graph'), bMid(53, 99, 'terraform graph | dot -Tpng'))))
    lines.append(R(merge(bMid(3, 50, 'State drift: plan shows changes'), bMid(53, 99, 'terraform refresh then plan'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'TF_LOG=DEBUG   = environment var; levels: TRACE, DEBUG, INFO, WARN, ERROR')))
    lines.append(R(bMid(3, 99, 'Cycle error    = circular dependency between resources; use depends_on carefully')))
    lines.append(R(bMid(3, 99, 'Already exists = resource in provider but not in state; fix: terraform import')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/troubleshooting/common-issues', 'docs/automation/terraform/troubleshooting/common-issues/index.md', 'Terraform common issues')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Common Issues'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Most frequent Terraform failures and their fixes')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Error acquiring the state lock')))
    lines.append(R(bMid(3, 99, 'Cause A: previous apply crashed → fix: terraform force-unlock <lock-id> from error message')))
    lines.append(R(bMid(3, 99, 'Cause B: concurrent apply running → fix: wait for it to complete; check CI pipeline')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Error: configuring Terraform AWS Provider')))
    lines.append(R(bMid(3, 99, 'Cause A: no credentials → fix: export AWS_PROFILE or configure OIDC in CI')))
    lines.append(R(bMid(3, 99, 'Cause B: wrong region → fix: set region in provider block or AWS_DEFAULT_REGION')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Issue: Error: resource already exists')))
    lines.append(R(bMid(3, 99, 'Fix: identify resource ID from error → terraform import resource.type.name <id>')))
    lines.append(R(bMid(3, 99, 'After import: terraform plan should show no changes if config matches the existing resource')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/troubleshooting/diagnostics', 'docs/automation/terraform/troubleshooting/diagnostics/index.md', 'Terraform diagnostics')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Diagnostics'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Terraform diagnostic sequence: capture logs → inspect plan JSON → check state → verify auth')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Log Capture'), bMid(53, 99, 'Plan Analysis'))))
    lines.append(R(merge(bMid(3, 50, 'TF_LOG=DEBUG tf plan &>debug.log'), bMid(53, 99, 'tf plan -out=p; tf show -json p'))))
    lines.append(R(merge(bMid(3, 50, 'TF_LOG_PROVIDER=DEBUG for API'), bMid(53, 99, 'cat plan.json | jq .resource_changes'))))
    lines.append(R(merge(bMid(3, 50, 'TF_LOG_PATH=./terraform.log'), bMid(53, 99, 'terraform state show <resource>'))))
    lines.append(R(merge(bMid(3, 50, 'terraform version (in logs)'), bMid(53, 99, 'terraform refresh (resync state)'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'TF_LOG_PATH    = write log to file instead of stderr; useful for CI artifact collection')))
    lines.append(R(bMid(3, 99, 'Plan JSON      = machine-readable plan; jq to find specific resource changes')))
    lines.append(R(bMid(3, 99, '.resource_changes= JSON field listing all planned changes with before/after values')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines


@kb_diagram('automation/terraform/troubleshooting/escalation', 'docs/automation/terraform/troubleshooting/escalation/index.md', 'Terraform escalation')
def _():
    R, txt_row = make_helpers(W2)
    lines = []
    lines.append(title_border(W2, 'Terraform — Escalation'))
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'Escalate Terraform issues: provider bugs → GitHub, state corruption → HashiCorp support')))
    lines.append(R(bMid(3, 99, 'Provider bugs: github.com/hashicorp/terraform-provider-<name>; include TF + provider version')))
    lines.append(R(bMid(3, 99, 'Terraform Cloud: support.hashicorp.com; TF Enterprise: emergency contact per SLA')))
    lines.append(R(bBot(3, 99)))
    lines.append(txt_row())
    lines.append(R(merge(bTop(3, 50), bTop(53, 99))))
    lines.append(R(merge(bMid(3, 50, 'Escalation Triggers'), bMid(53, 99, 'Info to Gather'))))
    lines.append(R(merge(bMid(3, 50, 'State file corrupted'), bMid(53, 99, 'terraform version output'))))
    lines.append(R(merge(bMid(3, 50, 'Provider panic / crash'), bMid(53, 99, 'Provider version'))))
    lines.append(R(merge(bMid(3, 50, 'Lock cannot be released'), bMid(53, 99, 'TF_LOG=TRACE full log'))))
    lines.append(R(merge(bMid(3, 50, 'TF Cloud outage'), bMid(53, 99, 'State file (sanitised)'))))
    lines.append(R(merge(bMid(3, 50, 'Sentinel policy blocking apply'), bMid(53, 99, 'Plan JSON output'))))
    lines.append(R(merge(bBot(3, 50), bBot(53, 99))))
    lines.append(txt_row())
    lines.append(R(bTop(3, 99)))
    lines.append(R(bMid(3, 99, 'State corruption   = restore from S3 versioned backup; terraform state push prev-state.json')))
    lines.append(R(bMid(3, 99, 'Provider GitHub    = github.com/hashicorp/terraform-provider-aws (replace aws with provider)')))
    lines.append(R(bMid(3, 99, 'HashiCorp support  = support.hashicorp.com; severity 1 for production outages')))
    lines.append(R(bBot(3, 99)))
    lines.append('└' + '─' * W2 + '┘')
    return lines
