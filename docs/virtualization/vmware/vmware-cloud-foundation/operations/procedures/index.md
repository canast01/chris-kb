# VCF — Procedures


<div class="kb-summary">
Procedures reference covering SDDC Manager Backup, Password Management, Useful Log Locations.
</div>

VCF Operational Procedures — Decision Map
```text
```
┌───────────────────────────── VMware Cloud Foundation — Common Procedures ─────────────────────────────┐
│                                                                                                       │
│  Routine VCF procedures: add host to free pool, create workload domain, rotate                        │
│  passwords, renew certificates, and commission/decommission domains.                                  │
│                                                                                                       │
│   ┌──────────────────────────────────────────────┐  ┌─────────────────────────────────────────────┐   │
│   │               Host Management                │  │              Domain Management              │   │
│   │         Commission host to free pool         │  │            Create workload domain           │   │
│   │         Validate: HCL + connectivity         │  │            Select hosts from pool           │   │
│   │          Assign to workload domain           │  │           SDDC Mgr deploys VC+NSX           │   │
│   │          Remove: decommission first          │  │           Decommission: drain VMs           │   │
│   └──────────────────────────────────────────────┘  └─────────────────────────────────────────────┘   │
│                                                                                                       │
│  Commission adds hardware capacity; workload domain creation consumes free pool hosts.                │
│                                                                                                       │
│                          ▼                                                 ▼                          │
│                                                                                                       │
│   ┌──────────────────────────────────────────────┐  ┌─────────────────────────────────────────────┐   │
│   │          Credential & Cert Rotation          │  │               Cluster Scaling               │   │
│   │         Rotate: SDDC Mgr > Security          │  │         Expand: add hosts to domain         │   │
│   │        Remediate: fix stuck rotation         │  │           Contract: drain + remove          │   │
│   │           Certs: Certificates tab            │  │            Add cluster to domain            │   │
│   │            CSR: generate + import            │  │         Delete cluster: drain first         │   │
│   └──────────────────────────────────────────────┘  └─────────────────────────────────────────────┘   │
│                                                                                                       │
│  Physical Infrastructure (the hardware everything above runs on):                                     │
│  Adding hosts requires physical cabling, BIOS config, and SDDC Mgr commissioning;                     │
│  password rotation may cause brief component restarts.                                                │
│                                                                                                       │
│  Key terms:                                                                                           │
│                                                                                                       │
│  Commission    = add physical host to SDDC Mgr free pool                                              │
│  Decommission  = remove host from VCF; must drain first                                               │
│  Free pool     = hosts available for domain assignment                                                │
│  Workload domain= isolated cluster with its own vCenter + NSX                                         │
│  Credential rotation= SDDC Mgr rotates passwords for all managed components                           │
│  CSR           = Certificate Signing Request; generated by SDDC Mgr                                   │
│  Remediate     = fix stuck lifecycle task; retry failed step                                          │
│  Expand cluster= add hosts to existing cluster in a domain                                            │
│  Contract      = reduce cluster size; hosts return to free pool                                       │
│  Drain         = migrate VMs off host before decommission                                             │
│  Security tab  = SDDC Mgr area for credential rotation and cert management                            │
│  Certificate tab= SDDC Mgr UI for cert status, CSR, and import                                        │
│                                                                                                       │
└───────────────────────────────────────────────────────────────────────────────────────────────────────┘
```
       │               │                │
       ▼               ▼                ▼
┌──────────────┐ ┌──────────────┐ ┌────────────────┐
│ Patching /   │ │ Password     │ │ Certificate                                                         │
│ Upgrading    │ │ Management   │ │ Management                                                          │
│              │ │              │ │                                                                     │
│ SDDC Mgr     │ │ SDDC Mgr     │ │ SDDC Mgr                                                            │
│ → LCM        │ │ → Security   │ │ → Security                                                          │
│ → Upgrade    │ │ → Password   │ │ → Certificate                                                       │
│              │ │   Management │ │   Management                                                        │
│ Order:       │ │              │ │                                                                     │
│ SDDC Mgr     │ │ Rotate via   │ │ Generate CSR                                                        │
│ → vCenter    │ │ SDDC Mgr     │ │ → sign → import                                                     │
│ → ESXi       │ │ (not direct  │ │                                                                     │
│ → NSX → vSAN │ │ in product)  │ │ Order:                                                              │
│              │ │              │ │ SDDC Mgr                                                            │
│ Pre-check    │ │ Update vault │ │ → vCenter                                                           │
│ first!       │ │ after rotate │ │ → NSX → ESXi                                                        │
└──────────────┘ └──────────────┘ └────────────────┘
```text
┌───────────────────────────── VMware Cloud Foundation — Common Procedures ─────────────────────────────┐
│                                                                                                       │
│  Routine VCF procedures: add host to free pool, create workload domain, rotate                        │
│  passwords, renew certificates, and commission/decommission domains.                                  │
│                                                                                                       │
│   ┌──────────────────────────────────────────────┐  ┌─────────────────────────────────────────────┐   │
│   │               Host Management                │  │              Domain Management              │   │
│   │         Commission host to free pool         │  │            Create workload domain           │   │
│   │         Validate: HCL + connectivity         │  │            Select hosts from pool           │   │
│   │          Assign to workload domain           │  │           SDDC Mgr deploys VC+NSX           │   │
│   │          Remove: decommission first          │  │           Decommission: drain VMs           │   │
│   └──────────────────────────────────────────────┘  └─────────────────────────────────────────────┘   │
│                                                                                                       │
│  Commission adds hardware capacity; workload domain creation consumes free pool hosts.                │
│                                                                                                       │
│                          ▼                                                 ▼                          │
│                                                                                                       │
│   ┌──────────────────────────────────────────────┐  ┌─────────────────────────────────────────────┐   │
│   │          Credential & Cert Rotation          │  │               Cluster Scaling               │   │
│   │         Rotate: SDDC Mgr > Security          │  │         Expand: add hosts to domain         │   │
│   │        Remediate: fix stuck rotation         │  │           Contract: drain + remove          │   │
│   │           Certs: Certificates tab            │  │            Add cluster to domain            │   │
│   │            CSR: generate + import            │  │         Delete cluster: drain first         │   │
│   └──────────────────────────────────────────────┘  └─────────────────────────────────────────────┘   │
│                                                                                                       │
│  Physical Infrastructure (the hardware everything above runs on):                                     │
│  Adding hosts requires physical cabling, BIOS config, and SDDC Mgr commissioning;                     │
│  password rotation may cause brief component restarts.                                                │
│                                                                                                       │
│  Key terms:                                                                                           │
│                                                                                                       │
│  Commission    = add physical host to SDDC Mgr free pool                                              │
│  Decommission  = remove host from VCF; must drain first                                               │
│  Free pool     = hosts available for domain assignment                                                │
│  Workload domain= isolated cluster with its own vCenter + NSX                                         │
│  Credential rotation= SDDC Mgr rotates passwords for all managed components                           │
│  CSR           = Certificate Signing Request; generated by SDDC Mgr                                   │
│  Remediate     = fix stuck lifecycle task; retry failed step                                          │
│  Expand cluster= add hosts to existing cluster in a domain                                            │
│  Contract      = reduce cluster size; hosts return to free pool                                       │
│  Drain         = migrate VMs off host before decommission                                             │
│  Security tab  = SDDC Mgr area for credential rotation and cert management                            │
│  Certificate tab= SDDC Mgr UI for cert status, CSR, and import                                        │
│                                                                                                       │
└───────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

- Schedule: daily; retain at least 7 restore points
- On-demand: `SDDC Manager → Administration → Backup → Backup Now`

## Password Management

```text
SDDC Manager → Security → Password Management
```

**Break-glass rotation procedure:**

1. Retrieve the break-glass account password from the enterprise vault
2. Rotate in SDDC Manager → Password Management
3. Update the vault entry immediately
4. Log the rotation in the change management system

## Useful Log Locations

| Component | Log Path |
|---|---|
| SDDC Manager | `/var/log/vmware/vcf/sddc-manager/` |
| LCM service | `/var/log/vmware/vcf/lcm/` |
| Domain manager | `/var/log/vmware/vcf/domainmanager/` |
| NSX Manager | NSX Manager UI → System → Support Bundle |
| ESXi (per host) | `/var/log/hostd.log`, `/var/log/vmkernel.log` |
