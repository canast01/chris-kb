# FOD — Access Control

> Part of the [Flex on Demand](../../) reference.

---

- Restrict Unisphere and APEX Console access to named storage administrators
- Use read-only roles for capacity monitoring automation accounts
- Apply least-privilege: FOD capacity queries do not require StorageAdmin write access — use a Viewer or Monitor role where available
- Audit APEX Console user access quarterly and remove stale accounts
